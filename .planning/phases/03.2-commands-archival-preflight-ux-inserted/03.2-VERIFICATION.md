---
phase: 03.2-commands-archival-preflight-ux-inserted
verified: 2026-04-16T16:35:00Z
status: passed
score: 10/10 must-haves verified
overrides_applied: 0
re_verification:
  previous_status: null
  previous_score: null
  gaps_closed: []
  gaps_remaining: []
  regressions: []
---

# Phase 3.2: Commands Archival + Preflight UX — Verification Report

**Phase Goal:** Close the two manual-QA bugs from Phase 3.1: (a) wire `command` ghosts into the bust pipeline; (b) hoist the running-Claude-process preflight BEFORE the tabbed picker; (c) hide the HOOKS tab.
**Verified:** 2026-04-16T16:35:00Z
**Status:** passed
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths

| #   | Truth | Status | Evidence |
| --- | ----- | ------ | -------- |
| SC1 | Commands archive end-to-end (select → archive → restore round-trip) | ✓ VERIFIED | `command-archival.test.ts` SC1 passes (710ms); asserts source .md removed, archive .md created, `archiveOp.category === 'command'`, restore reverses both |
| SC2 | `buildChangePlan` has `command` branch | ✓ VERIFIED | `change-plan.ts:82`: `if (r.item.category === 'command') { if (r.tier === 'definite-ghost') archive.push(...) }` |
| SC3 | `ChangePlan.counts.commands` required + rendered in both Unicode/ASCII confirmation + `renderChangePlan` emits commands row | ✓ VERIFIED | `change-plan.ts:43` required field; `confirmation.ts:79/97/136` nCommands + ASCII + Unicode rows; `tables/change-plan.ts:87` commands row; `command-archival.test.ts` SC3a (JSON counts.commands) + SC3b (tight regex) pass |
| SC4 | Entry preflight runs BEFORE `writeCheckpoint` and `selectGhosts` | ✓ VERIFIED | `ghost.ts:275` `runPreflightRetryLoop({ phase: 'entry' })` precedes `writeCheckpoint` at `ghost.ts:326`; awk-confirmed ordering |
| SC5 | Preflight copy reused verbatim via shared `renderRunningProcessMessage`; both interactive and `--dangerously-bust-ghosts` paths call helper; integration test asserts byte-identical stderr | ✓ VERIFIED | `_preflight-copy.ts:67` renderer + inline-snapshot SC5 drift guard; `ghost.ts` non-interactive switch refactored to `process.stderr.write(renderRunningProcessMessage(...))`; `preflight-retry.test.ts` SC5 passes (1146ms) — both paths `.toContain(expected)` |
| SC5a | Entry retry prompt via `@clack/prompts.confirm`; self-invocation exits 0 without retry | ✓ VERIFIED | `_preflight-copy.ts` runPreflightRetryLoop short-circuits on `initialResult.selfInvocation` without calling detectFn; 13/13 unit tests pass (incl. snapshot) |
| SC5b | Bust-time retry loop preserves `selectedItems` Set identity across 2+ retries without picker re-open | ✓ VERIFIED | `ghost.ts:348` single `const selectedItems = pickOutcome.ids`; `ghost.ts:439` `while (result.status === 'running-process')` re-invokes runBust with same selectedItems; `preflight-retry.test.ts` SC5b passes (33048ms) — asserts `Retry preflight?` + `Retry bust?` both appear, `[PREFLIGHT_DIRTY]` marker ≥4, manifest selection_filter.ids matches original 2-item set verbatim |
| SC6 | HOOKS tab hidden — filtered AFTER WR-02 exhaustiveness guard | ✓ VERIFIED | `tabbed-picker.ts:111` `if (cat === 'hook') continue;` inserted AFTER `knownCategories.has(cat)` guard; WR-02 regression test asserts throw on unknown category still fires; `preflight-retry.test.ts` SC6 passes (665ms) |
| SC7 | "Hook archival deferred" advisory in `ccaudit ghost` output, suppressed under --json/--csv/--quiet/--ci | ✓ VERIFIED | `ghost.ts:1863` `colorize.dim('Hook archival deferred — selectable archive coming in a future phase')` behind explicit 4-mode gate; `preflight-retry.test.ts` SC7 passes (911ms) — all 4 suppression modes asserted |
| SC8 | No regression — `pnpm verify` exits 0; Phase 2/3/3.1 regression pass; `dependencies: {}` empty; bundle delta <10 KB phase-local + <15 KB aggregate | ✓ VERIFIED | `pnpm verify` exit 0; 1384 tests passing (109 files); bundle actual=174968B baseline=170619B delta=4349B (within 10240B phase-local + 15360B aggregate budgets); `jq '.dependencies // {} | length'` returns 0 for all 3 packages |

**Score:** 10/10 truths verified

### Required Artifacts

| Artifact | Expected | Status | Details |
| -------- | -------- | ------ | ------- |
| `packages/internal/src/remediation/change-plan.ts` | command branch + counts.commands required | ✓ VERIFIED | L43 `commands: number;`, L82 command branch |
| `packages/internal/src/remediation/manifest.ts` | ArchiveOp.category widened | ✓ VERIFIED | L69 `category: 'agent' \| 'skill' \| 'command'` |
| `packages/internal/src/remediation/bust.ts` | archiveOne 'command' category + BustCounts.commands | ✓ VERIFIED | L124 commands counter, L385 commandItems loop, L617/690 segment ternary |
| `packages/internal/src/remediation/restore.ts` | command unarchive step | ✓ VERIFIED | L632 `archiveOps.filter((o) => o.category === 'command')` |
| `packages/terminal/src/tui/confirmation.ts` | commands row Unicode + ASCII | ✓ VERIFIED | L79 nCommands; L97 ASCII; L136 Unicode |
| `packages/terminal/src/tables/change-plan.ts` | commands row in renderChangePlan ARCHIVE | ✓ VERIFIED | L73 gate extended, L87 commands row |
| `packages/terminal/src/tui/_preflight-copy.ts` | renderRunningProcessMessage + runPreflightRetryLoop | ✓ VERIFIED | L67 renderer, L105 retry loop, 13/13 in-source tests pass |
| `packages/terminal/src/tui/tabbed-picker.ts` | hook skip after WR-02 guard | ✓ VERIFIED | L111 `if (cat === 'hook') continue;` after knownCategories check |
| `apps/ccaudit/src/cli/commands/ghost.ts` | entry preflight + bust retry loop + non-interactive refactor + advisory | ✓ VERIFIED | L275 entry preflight (phase:'entry'), L348 const selectedItems, L439 retry loop (phase:'bust'), L1863 advisory, inline copy removed |
| `apps/ccaudit/src/__tests__/command-archival.test.ts` | SC1 + SC3a + SC3b | ✓ VERIFIED | 3 tests, all passing |
| `apps/ccaudit/src/__tests__/preflight-retry.test.ts` | SC5 + SC5b + SC6 + SC7 | ✓ VERIFIED | 4 tests, all passing (SC5b 33s driver loop) |
| `apps/ccaudit/scripts/bundle-baseline-03.2.txt` | 170619 baseline | ✓ VERIFIED | File exists, contains `170619\n` |
| `package.json` verify script wired | CCAUDIT_PHASE_BASELINE + PHASE_BUDGET_BYTES | ✓ VERIFIED | verify stdout emits `[bundle-size] phase-local baseline=170619B delta=4349B budget=10240B` |

### Key Link Verification

| From | To | Via | Status |
| ---- | -- | --- | ------ |
| Entry preflight (ghost.ts:275) | runPreflightRetryLoop | phase:'entry' | ✓ WIRED |
| Bust-time retry (ghost.ts:439) | runPreflightRetryLoop | phase:'bust', preserves selectedItems | ✓ WIRED |
| Non-interactive switch (running-process case) | renderRunningProcessMessage | process.stderr.write | ✓ WIRED — inline copy removed |
| TabbedGhostPicker grouping loop | hook filter | after knownCategories.has() guard | ✓ WIRED |
| ghost.ts text-mode | advisory stdout | 4-mode gate (json/csv/quiet/ci) | ✓ WIRED |
| runBust plan.archive | archiveOne with category='command' | filteredPlan.archive.filter(i.category==='command') | ✓ WIRED |

### Requirements Coverage

| Requirement | Source Plan | Status | Evidence |
| ----------- | ----------- | ------ | -------- |
| TUI-01 (refined) | Plans 01-05 | ✓ SATISFIED | Commands archive + preflight hoist + HOOKS hidden — all 4 integration tests + 9 unit tests green |
| TUI-05 (partial) | Plans 01-05 | ✓ SATISFIED | Preflight UX ordering, retry semantics, self-invocation handling — verified via preflight-retry.test.ts |

### Anti-Patterns Found

None. No TODO/FIXME/placeholder markers introduced. No hardcoded empty data in rendered output. No stub handlers.

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
| -------- | ------- | ------ | ------ |
| Full verify gate | `pnpm verify` | exit 0, 1384 tests pass, format:check clean | ✓ PASS |
| Bundle gate (aggregate) | bundle-size-check.mjs | `actual=174968B baseline=170619B delta=4349B budget=15360B` | ✓ PASS |
| Bundle gate (phase-local) | bundle-size-check.mjs | `phase-local baseline=170619B delta=4349B budget=10240B` | ✓ PASS |
| Zero runtime deps | `jq '.dependencies // {} | length'` | 0 for all 3 packages | ✓ PASS |
| SC1/SC3 integration | command-archival.test.ts | 3/3 tests pass | ✓ PASS |
| SC5/SC5b/SC6/SC7 integration | preflight-retry.test.ts | 4/4 tests pass | ✓ PASS |

### Human Verification Required

None. All 8 (+2 sub-criteria) must-haves have automated test coverage at the unit, integration, and subprocess levels. The SC5b integration test exercises the real clack prompt + real subprocess + real manifest state — no human visual confirmation is needed.

### Gaps Summary

None. Every success criterion is satisfied by:
1. Unit tests (in-source vitest) for pure logic (change-plan, confirmation, _preflight-copy, tabbed-picker).
2. Subprocess integration tests for end-to-end flows (command-archival, preflight-retry).
3. `pnpm verify` green gate covering typecheck, lint, build, bundle-size, tests, format.
4. Bundle delta (4349B) well within both budgets.
5. Zero-runtime-deps invariant preserved.

---

_Verified: 2026-04-16T16:35:00Z_
_Verifier: Claude (gsd-verifier)_
