---
phase: 03.2-commands-archival-preflight-ux-inserted
fixed_at: 2026-04-16
review_path: .planning/phases/03.2-commands-archival-preflight-ux-inserted/03.2-REVIEW.md
iteration: 1
scope: critical-and-warning
findings_in_scope: 2
fixed_count: 2
skipped_count: 4
status: all_resolved
skipped_reason:
  IN-01: out-of-scope (info-only; scope excludes info findings)
  IN-02: out-of-scope (info-only; scope excludes info findings)
  IN-03: out-of-scope (info-only; scope excludes info findings)
  IN-04: out-of-scope (info-only; scope excludes info findings)
verify_exit_code: 0
---

# Phase 3.2 Code Review Fix Report

**Fixed at:** 2026-04-16
**Source review:** 03.2-REVIEW.md
**Iteration:** 1
**Scope:** critical-and-warning (info findings excluded)

## Summary

- Findings in scope: 2 (0 critical + 2 warning)
- Fixed: 2
- Skipped: 4 (all info, out of scope)
- `pnpm verify` status: exit 0 — typecheck + lint + build + test (1384 passed, 2 skipped, 1 todo across 109 test files) + format:check all green.

## Fixed Issues

### WR-01: `runPreflightRetryLoop` retry iteration pid drift not covered by tests

**Files modified:** `packages/terminal/src/tui/_preflight-copy.ts`
**Commits:** `18d95b2` (test added), `a5408e6` (oxfmt reflow)
**Applied fix:** Added in-source test `WR-01: on retry iteration 2, rendered pids come from detectFn output, not stale initialResult` inside the existing `describe('runPreflightRetryLoop', ...)` block. The test supplies `initialResult = { selfInvocation: false, pids: [999] }`, has `detectFn` return fresh `[{ pid: 888 }]` on first call and empty on second, confirms user retry via a `confirm` mock resolving `true`, then spies on `process.stderr.write` to assert: (a) iter-1 render contains `pids: 999`, (b) iter-2 render contains `pids: 888`, (c) no render AFTER iter-1 still references the stale `999`. Locks the invariant that fresh detection supersedes the stale initialResult on every retry iteration.

### WR-02: Entry preflight detect counter semantics are subtle

**Files modified:** `apps/ccaudit/src/cli/commands/ghost.ts`
**Commit:** `8a993f2`
**Applied fix:** Added a 3-line inline comment immediately before the initial `detectClaudeProcesses` call inside `runInteractiveGhostFlow` (previously at line 253) explaining the DIRTY-counter accounting: the initial detect consumes one `CCAUDIT_TEST_PREFLIGHT_DIRTY` counter tick on `entryProcessDeps`; each user-confirmed retry inside `runPreflightRetryLoop` consumes one more. Future readers debugging off-by-one behavior with the env-hook counter now have in-situ context.

## Skipped Issues

All skipped findings are info-severity and fall outside the configured scope (`critical-and-warning`). None are blocking; they can be addressed in a future polish pass.

### IN-01: Possibly-unused `mode.ci` field on the interactive mode object
**Reason:** out-of-scope (info).

### IN-02: Outdated line-number anchor in `_preflight-copy.ts` doc comment
**Reason:** out-of-scope (info). Line-number drift is cosmetic; the inline snapshot at ~line 182 remains the real drift guard.

### IN-03: `renderConfirmationScreen` uses "1 commands" / "1 agents" / "1 memory"
**Reason:** out-of-scope (info). Semi-breaking for locked test snapshots; revisit in a dedicated polish phase.

### IN-04: `CCAUDIT_TEST_STDOUT_ROWS` escape hatch not cross-referenced in env-hook comment
**Reason:** out-of-scope (info).

---

_Fixed: 2026-04-16_
_Fixer: Claude (gsd-code-fixer)_
_Iteration: 1_
