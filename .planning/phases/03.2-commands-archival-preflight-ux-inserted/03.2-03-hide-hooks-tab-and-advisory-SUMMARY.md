---
phase: 03.2-commands-archival-preflight-ux-inserted
plan: 03
subsystem: tui
tags: [tui, tabbed-picker, hooks, advisory, SC6, SC7, WR-02, D-23, suppression-matrix]

# Dependency graph
requires:
  - phase: 03.1-tabbed-category-view
    plan: 02
    provides: "TabbedGhostPicker constructor grouping loop + WR-02 exhaustiveness guard — both the insertion point and the invariant this plan preserves"
  - phase: 03.1-tabbed-category-view
    plan: 03
    provides: "CATEGORY_ORDER / CATEGORY_LABEL single-source-of-truth in `packages/terminal/src/tui/select-ghosts.ts` — confirms we skip at the consumer (picker) rather than the taxonomy"
  - phase: 02-tui-picker-v0.5
    provides: "D-23 4-mode suppression matrix (json/csv/quiet/ci) — the advisory gate mirrors this exact shape"
provides:
  - "SC6: HOOKS tab structurally impossible to render — `cat === 'hook'` items are filtered out of the TabbedGhostPicker grouping loop AFTER the WR-02 guard"
  - "SC7: one-line advisory `Hook archival deferred — selectable archive coming in a future phase` surfaces in `ccaudit ghost` text output, gated on the full 4-mode D-23 suppression matrix"
  - "In-source regression tests (3) covering: hook-tab filtering, WR-02 unknown-category throw, only-hook-input empty-tabs throw"
affects:
  - "03.2-04 (CLI preflight wiring): no new blockers — the hook filter is a one-liner inside the picker constructor, entirely orthogonal to preflight/retry concerns"
  - "03.2-05 (integration tests + bundle gate): the advisory line is the only user-visible text addition in this plan (dim stderr); impact on bundle size is sub-byte (a single string literal)"
  - "Future 'Hook archival' phase: CATEGORY_ORDER still contains 'hook' (intentional — WR-02 protects against future scanner additions landing without a wired tab). When hook archival semantics are designed, the skip line is the single point to remove."

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Consumer-side category filtering: remove category from the picker's grouping loop rather than from CATEGORY_ORDER — preserves WR-02 protection against future domain-type expansion landing silently"
    - "4-mode D-23 suppression gate applied defensively inside a text-mode-only branch: `!mode.json && !mode.csv` is structurally implied, but checked explicitly so the gate is self-documenting and robust to future restructuring of the outer if/else-if chain"
    - "TDD RED/GREEN commit split: test-first commit establishes failing expectations, implementation commit resolves them + adjusts sibling fixture data that relies on the old contract"

key-files:
  created: []
  modified:
    - packages/terminal/src/tui/tabbed-picker.ts
    - apps/ccaudit/src/cli/commands/ghost.ts

key-decisions:
  - "Kept `'hook'` in CATEGORY_ORDER and in the `knownCategories` Set. Filtering only the grouping-loop push preserves WR-02's strict exhaustiveness guard — a future scanner addition that emitted hook items without wiring a tab would still fail loud. Loosening the taxonomy would make hook-archival work harder to land cleanly in a later phase."
  - "Added an EXPLICIT 4-mode suppression gate (`!mode.json && !mode.csv && !mode.quiet && ctx.values.ci !== true`) even though the insertion site is already inside the text-mode arm of the outer `if (mode.json) { ... } else if (mode.csv) { ... } else { ... }` chain. Rationale: the outer arm structurally implies `!json && !csv` but does NOT imply `!quiet` (which is a per-line gate) or `!ci` (which lives on `ctx.values.ci`, not `mode`). Relying solely on the outer structure would be a silent regression risk under a future restructure."
  - "Updated Tests 1, 5, 6, 7 in the in-source `describe('TabbedGhostPicker', ...)` block: swapped `category: 'hook'` fixture rows for `category: 'command'` so the tab-cycling / numeric-jump / label invariants continue to exercise exactly 3 tabs. The tests' behavioral intent (tabs cycle, numeric N-1 jumps, wrap semantics) is unrelated to hook-ness — the fixture swap preserves coverage without adding new test surface."
  - "Added the advisory block as a SIBLING of the existing `renderHooksAdvisory` block (same 8-space indent), not as a nested branch inside `!includeHooks`. The advisory fires independently of the `--include-hooks` mode — hooks are always deferred regardless of whether they render in the main table or in the boxed advisory."

patterns-established:
  - "When a taxonomy enum is shared across consumers, prefer per-consumer filtering over enum editing when a subset is temporarily unsupported — the enum stays the safety contract (WR-02 guard), and only the consumer opts out."
  - "Text-mode advisory lines must check all four D-23 modes (json/csv/quiet/ci) explicitly when they live in a text-mode arm — the outer structural branch only covers json/csv; quiet and ci are per-line gates."

requirements-completed: [TUI-01, TUI-05]

# Metrics
duration: 4min
completed: 2026-04-16
---

# Phase 03.2 Plan 03: Hide HOOKS tab + advisory note Summary

**Two surgical edits close SC6 and SC7: a one-line `if (cat === 'hook') continue;` skip in the `TabbedGhostPicker` constructor hides the HOOKS tab structurally without loosening WR-02, and a one-line advisory (`Hook archival deferred — selectable archive coming in a future phase`) surfaces in `ccaudit ghost` text output behind an explicit 4-mode suppression gate.**

## Performance

- **Duration:** ~4 min (210 s)
- **Started:** 2026-04-16T15:21:47Z
- **Completed:** 2026-04-16T15:25:17Z
- **Tasks:** 2 (Task 1 TDD; Task 2 straight implementation)
- **Files created:** 0
- **Files modified:** 2 (packages/terminal/src/tui/tabbed-picker.ts, apps/ccaudit/src/cli/commands/ghost.ts)

## Accomplishments

- **SC6 locked (Task 1):** `TabbedGhostPicker` constructor now skips `category === 'hook'` items AFTER the WR-02 `knownCategories` guard and BEFORE `grouped[cat].push(g)`. Combined with Phase 3.1's existing D3.1-04 empty-tabs-dropped behavior, the HOOKS tab is structurally impossible to render. WR-02 exhaustiveness guard continues to throw for any category outside CATEGORY_ORDER.
- **3 new in-source tests (Task 1):** `hook-category items are filtered out of tabs entirely` (asserts `ids` excludes `'hook'`, length is 3 not 4), `WR-02 exhaustiveness guard still fires for unknown categories` (asserts throw on `'not-a-category'`), `input of ONLY hook items throws the empty-tabs guard` (asserts the Phase 3.1 `no non-empty categories` error surfaces).
- **SC7 locked (Task 2):** `ccaudit ghost` text output now prints a single dim line `Hook archival deferred — selectable archive coming in a future phase` when hook ghosts exist and the user is not in any of the 4 suppression modes (`--json`, `--csv`, `--quiet`, `--ci`). The outer structural `if (mode.json) / else if (mode.csv) / else { ... }` arm already covers json/csv, but the explicit gate adds `!mode.quiet && ctx.values.ci !== true` so the full D-23 matrix is honored.
- **Test suite green across the board:** all 16 `tabbed-picker.ts` in-source tests pass (13 pre-existing + 3 new). Full terminal test suite (333 tests across 21 files) passes. `tsc --noEmit` exits 0 for both `@ccaudit/terminal` and `ccaudit-cli`.
- **Fixture-swap preserves existing coverage:** Tests 1/5/6/7 had hardcoded `category: 'hook'` rows whose purpose was to produce a 3rd tab. I swapped those to `category: 'command'` — same tab count, same cycling semantics, just exercising a different category column. No tests were removed.
- **CATEGORY_ORDER and `knownCategories` Set unchanged:** `grep -c "'hook'" packages/terminal/src/tui/select-ghosts.ts` returns `1` (line 65 in the `CATEGORY_ORDER` tuple) — matching the pre-plan baseline. The WR-02 safety contract is intact.

## Verification Evidence

```
$ pnpm --filter @ccaudit/terminal exec vitest run src/tui/tabbed-picker.ts --reporter=default
 ✓ src/tui/tabbed-picker.ts (16 tests) 5ms
 Test Files  1 passed (1)
      Tests  16 passed (16)

$ pnpm --filter @ccaudit/terminal exec vitest run --reporter=default
 Test Files  21 passed (21)
      Tests  333 passed (333)

$ pnpm --filter @ccaudit/terminal exec tsc --noEmit ; echo $?
0
$ pnpm --filter ccaudit-cli typecheck ; echo $?
0

$ grep -q "if (cat === 'hook') continue;" packages/terminal/src/tui/tabbed-picker.ts && echo OK
OK
$ awk '/knownCategories.has\(cat\)/{g=NR} /cat === .hook.*continue/{h=NR} END{exit !(h>g)}' \
    packages/terminal/src/tui/tabbed-picker.ts && echo OK
OK
$ awk '/hookItems.length > 0/,/Hook archival deferred/{print}' \
    apps/ccaudit/src/cli/commands/ghost.ts \
  | grep -c '!mode.json\|!mode.csv\|!mode.quiet\|ctx.values.ci !== true'
4
```

## Deviations from Plan

None — plan executed exactly as written.

The only judgment calls were:

1. **Existing-test fixture swap (Tests 1, 5, 6, 7):** the plan said "preserve every existing test — these are additive" but Tests 1/5/6/7 had `category: 'hook'` rows baked into their fixtures (to produce a 3-tab layout). Under the new contract those tests would fail structurally (2 tabs, not 3). I swapped `'hook'` → `'command'` in four places, preserving the tests' intent (cycling/numeric-jump over 3 tabs). This is consistent with the plan's intent — the tests remain, they just use a different 3rd category.
2. **Advisory block placement:** the plan said "append a NEW block immediately after the closing `}` of that `if (!includeHooks) { ... } else if (hooksUpperBound > 0) { ... }` — i.e., at approximately line 1708." I placed it exactly there with a blank line separator, matching the 8-space indent of the `if (!includeHooks)` block as instructed.

## Commits

- `732a715` test(03.2-03): RED — add SC6 hook-tab filter + WR-02 preservation tests
- `a1165f0` feat(03.2-03): GREEN — skip hook-category items in TabbedGhostPicker grouping loop
- `f0fc6ce` feat(03.2-03): add hook-archival-deferred advisory to ccaudit ghost text output

## Known Stubs

None. Both edits are complete and behaviorally active in this plan's output — no TODO/FIXME markers, no placeholder data paths.

## Self-Check: PASSED

- [x] `packages/terminal/src/tui/tabbed-picker.ts` — exists, modified, contains `if (cat === 'hook') continue;` AFTER the WR-02 guard
- [x] `apps/ccaudit/src/cli/commands/ghost.ts` — exists, modified, contains the 4-mode-gated advisory block
- [x] Commit `732a715` — present in `git log`
- [x] Commit `a1165f0` — present in `git log`
- [x] Commit `f0fc6ce` — present in `git log`
- [x] All 16 tabbed-picker tests pass; full 333-test terminal suite passes; both tsc checks exit 0
