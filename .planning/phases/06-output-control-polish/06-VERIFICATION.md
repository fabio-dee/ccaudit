---
phase: 06-output-control-polish
verified: 2026-04-05T06:50:00Z
status: passed
score: 7/7 ROADMAP truths verified, all 6 historical gaps closed (Gaps #1–#6)
re_verification:
  previous_status: gaps_found
  previous_verified: 2026-04-05T06:00:00Z
  previous_score: 7/7 ROADMAP truths verified, 4 escaped gaps discovered during first end-to-end manual test
  gaps_closed:
    - "Gap #3 — JSON schema discoverability closed by Plan 06-07: docs/JSON-SCHEMA.md (101 lines) documents D-16 camelCase envelope + per-command payload keys + projectPaths traceability note + jq recipes; README has Machine-readable output section linking to schema; all 4 subcommand --json descriptions reference JSON-SCHEMA.md; docs/manual-test-results.md corrected (row 5 PASS + spec-clarification note)"
    - "Gap #4 — --no-color help visibility closed by Plan 06-07: 'no-color' declared in outputArgs at _shared-args.ts (additive gunshi help metadata only); initColor() in color.ts UNCHANGED and still reads process.argv directly as authoritative runtime source (D-07 preserved); in-source test flipped from 'not toHaveProperty' to positive assertions + NO_COLOR env mention check; E2E help-output.test.ts spawns dist/index.js and asserts all 5 --help outputs contain 'no-color'"
    - "Gap #5 — mcp --csv cross-project dedup closed by Plan 06-07: new aggregateMcpByName() helper in commands/mcp.ts groups by server name with least-ghost tier precedence, max lastUsed, summed invocationCount; projectPaths: string[] exposed in JSON items for traceability; scanner per-project (projectPath::serverName) dedup UNCHANGED (Phase 8 RMED-06 preserved); 3 E2E regression tests in mcp-aggregation.test.ts"
    - "Gap #6 — pnpm -r build from subpackage dirs closed by Plan 06-07: 'build': 'tsc' stubs added to packages/internal/package.json and packages/terminal/package.json; idempotent via composite + emitDeclarationOnly tsconfig; pnpm -r build now processes 3 of 4 workspace projects cleanly from workspace root + both subpackage dirs with zero ERR_PNPM_RECURSIVE_RUN_NO_SCRIPT"
  gaps_remaining: []
  regressions: []
  invariants_preserved:
    - "D-16 JSON envelope: git diff 88af4a4..HEAD apps/ccaudit/src/cli/_output-mode.ts → 0 lines"
    - "D-07 initColor runtime: git diff 88af4a4..HEAD packages/terminal/src/color.ts → 0 lines"
    - "Phase 8 RMED-06 scanner: git diff 88af4a4..HEAD packages/internal/src/scanner/scan-mcp.ts → 0 lines"
  orchestrator_caught_regression:
    - "Task 5 initial commit 79b0382 used beforeAll to build dist/index.js, which raced with parallel dry-run-command.test.ts also spawning the same binary (tsdown cleans dist/ before writing → transient ENOENT). Caught by orchestrator spot-check before verification; fixed in 379db85 by switching to describe.skipIf(!binaryExists) and requiring prebuild as a CI prerequisite. Evidence that the execute-phase pipeline's own regression gate works."
  prior_re_verifications:
    - gaps_closed: ["Gap #1 — CI coverage invocation silent no-op (Plan 06-05)"]
    - gaps_closed: ["Gap #2 — commit e3dbe01 wiped scripts/devDependencies from apps/ccaudit/package.json (Plan 06-06)"]
---

# Phase 6: Output Control & Polish Verification Report

**Phase Goal:** Deliver production-ready output control for all 4 CLI commands — shared `--quiet`, `--csv`, `--ci` flags with exit code semantics, JSON meta envelope, CSV export, verbose-to-stderr routing, quiet TSV output, plus CI matrix with 80% coverage enforcement and publication-ready npm metadata.

**Verified:** 2026-04-05T06:50:00Z
**Status:** passed
**Re-verification:** Yes — third re-verification after Plan 06-07 gap closure (Gaps #3/#4/#5/#6). Prior re-verifications closed Gap #1 (Plan 06-05) and Gap #2 (Plan 06-06).

## Re-Verification Summary (2026-04-05 Round 3)

Plan 06-07 was a surgical 8-task gap-closure plan (10 commits + 1 orchestrator-caught regression fix = 11 total) translated from an approved three-angle parallel investigation. All 4 escaped gaps from the 2026-04-05T06:00:00Z manual-test verification are now closed with observable evidence, regression tests, and preserved invariants.

**Gap #3 (JSON schema discoverability) — CLOSED.** The code was always correct per D-16 (camelCase `items` / `meta.timestamp` / `meta.exitCode`); the gap was the absence of public documentation to falsify the tester's snake_case expectations. Plan 06-07 created `docs/JSON-SCHEMA.md` (101 lines) as the canonical reference with the envelope table, per-command payload keys (`items` for ghost/inventory/mcp, `buckets` for trend), a note on the new `mcp.items[].projectPaths` traceability field, jq recipes for all 4 commands, and a full worked example. `README.md` added a "Machine-readable output" section at line 148 with jq/CSV/--ci examples linking to the schema doc (line 166). All 4 subcommand files (`ghost.ts`, `inventory.ts`, `mcp.ts`, `trend.ts`) extended their `--json` arg description to read `'Output as JSON (see docs/JSON-SCHEMA.md for schema)'`. `docs/manual-test-results.md` summary row 5 flipped from FAIL to PASS with a spec-clarification pointer. No D-16 code changes — the contract remains frozen.

**Gap #4 (--no-color invisible in --help) — CLOSED.** Plan 06-07 added `'no-color'` to `outputArgs` in `_shared-args.ts` as a gunshi help-metadata declaration only. The runtime source of truth remains `initColor()` in `packages/terminal/src/color.ts` reading `process.argv.includes('--no-color')` directly (D-07 preserved, git diff shows 0 lines changed since plan start). Both sources agree because gunshi parsing does not mutate `process.argv`. The in-source test at `_shared-args.ts:73-84` was flipped from `expect(outputArgs).not.toHaveProperty('no-color')` to positive assertions (key present, type boolean, default false, description mentions NO_COLOR env var). A new E2E regression test at `apps/ccaudit/src/__tests__/help-output.test.ts` (5 tests) spawns `dist/index.js` and greps `--help` output for all 5 commands.

**Gap #5 (mcp --csv cross-project duplicates) — CLOSED.** Plan 06-07 added a new `aggregateMcpByName(enriched)` helper exported from `apps/ccaudit/src/cli/commands/mcp.ts` (line 47) that groups enriched results by server name and merges: tier via least-ghost precedence (`used` > `likely-ghost` > `definite-ghost`), `lastUsed` via max, `invocationCount` via sum, `projectPath` set to null, `projectPaths: string[]` populated with source paths (singletons get a single-element array, globals get empty array). Call site inserted at line 251 between live-measurement and output branching. JSON `items.map` extended at line 283 to include the new `projectPaths` field. Critically, `packages/internal/src/scanner/scan-mcp.ts` is **UNCHANGED** (git diff 0 lines) — per-project `(projectPath::serverName)` dedup is preserved for Phase 8 RMED-06 config-key traceability. 3 helper-level regression tests at `mcp-aggregation.test.ts` + 2 in-source tests in `mcp.ts` cover cross-project collapse, least-ghost tier merge, mixed-set counting, and single-instance passthrough.

**Gap #6 (pnpm -r build from subpackage dirs) — CLOSED.** Plan 06-07 added `"build": "tsc"` stubs to `packages/internal/package.json` and `packages/terminal/package.json`. These are idempotent because both tsconfig.json files already have `composite: true` + `emitDeclarationOnly: true`, so `tsc` only produces declarations. `pnpm -r build` from workspace root, from `packages/internal`, and from `packages/terminal` all exit 0 with zero `ERR_PNPM_RECURSIVE_RUN_NO_SCRIPT` occurrences.

**Orchestrator-caught regression (not counted as a gap).** Task 5's initial commit `79b0382` added a `beforeAll` hook that rebuilt `dist/index.js` inside the test — but that racing against `dry-run-command.test.ts` (which also spawns the same binary) produced transient ENOENT failures because tsdown cleans `dist/` before writing. The execute-phase pipeline's own spot-check caught this before verification. Commit `379db85` fixed it by switching to `describe.skipIf(!binaryExists)` and documenting that callers must `pnpm -F ccaudit build` before `pnpm test`. This is evidence that the execute-phase verification gate works as designed — not a gap in the v1.0 binary, but a hygiene improvement in the regression test itself.

See `.planning/phases/06-output-control-polish/06-07-SUMMARY.md` for detailed per-task outcomes, commit hashes, and deviation log.

## Goal Achievement

### Observable Truths (derived from ROADMAP Success Criteria)

| #   | Truth (ROADMAP SC)                                                                                                                              | Status     | Evidence                                                                                                                                                                                                                                                                                                                                             |
| --- | ----------------------------------------------------------------------------------------------------------------------------------------------- | ---------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1   | Exit code 0 when no ghosts, 1 when ghosts found (ghost/inventory/mcp); trend always 0                                                           | ✓ VERIFIED | `ghost --ci > /dev/null; echo $?` → EXIT=1. `trend --json > /dev/null; echo $?` → EXIT=0. Re-verified this session.                                                                                                                                                                                                                                  |
| 2   | `NO_COLOR` env and `--no-color` flag produce ANSI-free output; `--quiet` suppresses decoration; `--verbose` shows scan details                  | ✓ VERIFIED | `NO_COLOR=1 ghost --quiet \| cat -v` → 0 escape sequences. `--no-color` now ALSO visible in --help (Gap #4 closed). initColor() still reads process.argv directly (D-07 preserved, 0-line git diff).                                                                                                                                                 |
| 3   | `--ci` combines exit-code + quiet + JSON for GitHub Actions                                                                                     | ✓ VERIFIED | `ghost --ci` produces compact JSON envelope with `items` + `meta.timestamp` + `meta.exitCode` (all camelCase per D-16) and exits 1.                                                                                                                                                                                                                  |
| 4   | `--json` and `--csv` flags produce structured/spreadsheet-compatible export on all read commands                                                | ✓ VERIFIED | All 4 commands wrap output via `buildJsonEnvelope(command, since, exitCode, data)`. `mcp --csv` now emits zero duplicate rows (Gap #5 closed). `mcp --json` items include new `projectPaths: string[]` field for traceability. Schema now publicly documented at docs/JSON-SCHEMA.md (Gap #3 closed).                                                  |
| 5   | README, npm metadata, and package are publication-ready (v1.0 launch candidate)                                                                 | ✓ VERIFIED | README has "Machine-readable output" section (new line 148) + Flags Reference table + GitHub Actions example. `docs/JSON-SCHEMA.md` now part of the public documentation surface. All 4 command `--help` outputs reference the schema doc in their `--json` description.                                                                             |
| 6   | CI test job enforces 80% coverage threshold via `vitest --coverage`; fails if coverage drops                                                    | ✓ VERIFIED | Coverage config in `vitest.config.ts` (Plan 06-05). Current run: stmts 93.61%, branches 84.71%, fns 96%, lines 94.4% — all above thresholds (80/70/80/80) with double-digit margin. `coverage/coverage-summary.json` present.                                                                                                                          |
| 7   | CI matrix runs on ubuntu-latest and macos-latest; all jobs pass on both                                                                         | ✓ VERIFIED | `.github/workflows/ci.yaml` OS matrix unchanged. `pnpm -r build` now also succeeds from subpackage dirs (Gap #6 closed) so local/CI symmetry is improved even though CI only invokes from root.                                                                                                                                                      |

**Score:** 7/7 truths verified. All 6 historical gaps closed.

### Required Artifacts

| Artifact                                         | Expected                                                                                 | Status     | Details                                                                                                                                                                                                                                                                              |
| ------------------------------------------------ | ---------------------------------------------------------------------------------------- | ---------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| `docs/JSON-SCHEMA.md` *(NEW 06-07)*              | Canonical D-16 envelope reference + per-command payload keys + projectPaths note + jq    | ✓ VERIFIED | 101 lines. Contains `meta.command/version/since/timestamp/exitCode` table, per-command payload key table (items for ghost/inventory/mcp, buckets for trend), note explaining mcp.items[].projectPaths from Gap #5 fix, 4 jq recipe examples, 1 full worked example.                 |
| `README.md` *(MODIFIED 06-07)*                   | Machine-readable output section linking to docs/JSON-SCHEMA.md                           | ✓ VERIFIED | "Machine-readable output" section at line 148 with jq/CSV/--ci examples; line 166 contains `[JSON Schema](./docs/JSON-SCHEMA.md)` link.                                                                                                                                              |
| `apps/ccaudit/src/cli/_shared-args.ts`           | outputArgs with quiet/csv/ci AND now 'no-color' (Gap #4 fix)                             | ✓ VERIFIED | `'no-color'` key present at line 28 with type boolean, default false, description mentioning NO_COLOR env var. Header comment updated explaining two-source design. 7 in-source tests (2 flipped/added for Gap #4).                                                                  |
| `apps/ccaudit/src/cli/commands/mcp.ts`           | aggregateMcpByName helper + call site + projectPaths JSON field + in-source tests        | ✓ VERIFIED | `aggregateMcpByName` exported at line 47; called at line 251 between live-measurement and output branching; JSON items include `projectPaths` at line 283; 2 in-source tests (merge case + single-instance passthrough) at lines 392-450.                                            |
| `apps/ccaudit/src/cli/commands/ghost.ts`         | --json description references JSON-SCHEMA.md                                             | ✓ VERIFIED | Line 55: `description: 'Output as JSON (see docs/JSON-SCHEMA.md for schema)'`.                                                                                                                                                                                                       |
| `apps/ccaudit/src/cli/commands/inventory.ts`     | --json description references JSON-SCHEMA.md                                             | ✓ VERIFIED | Line 38: same description pattern.                                                                                                                                                                                                                                                   |
| `apps/ccaudit/src/cli/commands/trend.ts`         | --json description references JSON-SCHEMA.md                                             | ✓ VERIFIED | Line 38: same description pattern.                                                                                                                                                                                                                                                   |
| `packages/internal/package.json`                 | `"build": "tsc"` script stub                                                             | ✓ VERIFIED | `"build": "tsc"` present. Idempotent via tsconfig composite+emitDeclarationOnly.                                                                                                                                                                                                     |
| `packages/terminal/package.json`                 | `"build": "tsc"` script stub                                                             | ✓ VERIFIED | `"build": "tsc"` present.                                                                                                                                                                                                                                                            |
| `apps/ccaudit/src/__tests__/help-output.test.ts` *(NEW 06-07)* | 5 E2E tests spawning dist/index.js, greps --help for no-color          | ✓ VERIFIED | 5 `it(...)` blocks (root + 4 subcommands). Uses `fileURLToPath(import.meta.url)` path resolution (deviation from plan text, documented — the plan's `process.cwd()` would double-nest under `pnpm -F ccaudit` scope). `describe.skipIf(!binaryExists)` avoids build race (fixed in 379db85). |
| `apps/ccaudit/src/__tests__/mcp-aggregation.test.ts` *(NEW 06-07)* | 3 helper-level tests for aggregateMcpByName                         | ✓ VERIFIED | 3 `it(...)` blocks: cross-project collapse (3 paths → 1 row), least-ghost tier selection, mixed-set row count. Tests the exported helper directly for deterministic coverage.                                                                                                         |
| `docs/manual-test-results.md`                    | Row 5 flipped from FAIL to PASS with spec-clarification note                             | ✓ VERIFIED | Line 17: `\| 5. --json structured output \| PASS (spec clarified in [JSON-SCHEMA.md](./JSON-SCHEMA.md)) \|`. Full section referencing docs/JSON-SCHEMA.md at line 74.                                                                                                                 |
| `.planning/phases/06-output-control-polish/deferred-items.md` *(NEW 06-07)* | Ledger of 5 pre-existing lint errors out of scope           | ✓ VERIFIED | 41 lines. Lists `estimate.ts:69` (unused `unlink`), `scan-all.ts:5` (unused `InvocationSummary`), `apps/ccaudit/scripts/gen-version.mjs` (eslint tsconfig discovery). Documents root cause + recommended follow-up + v1.0 non-blocker status.                                         |
| *(existing artifacts from prior verification — all still VERIFIED)* | Original 20 artifact rows from 2026-04-04 verification                | ✓ VERIFIED | All artifacts from the prior passed verification (color.ts, csv.ts, quiet.ts, _output-mode.ts, all 4 command files, vitest.config.ts, ci.yaml, apps/ccaudit/package.json, dist/index.js, etc.) still verify — see 2026-04-04 row in the history.                                     |

### Key Link Verification

| From                                                 | To                                        | Via                                          | Status    | Details                                                                                                                              |
| ---------------------------------------------------- | ----------------------------------------- | -------------------------------------------- | --------- | ------------------------------------------------------------------------------------------------------------------------------------ |
| `apps/ccaudit/src/cli/_shared-args.ts` (outputArgs)  | All 4 command files                       | `...outputArgs` spread (existing)            | ✓ WIRED   | `--no-color` now automatically flows to every subcommand via the existing spread pattern — no new wiring needed for Gap #4.          |
| `apps/ccaudit/src/cli/commands/mcp.ts`               | `aggregateMcpByName` helper               | call at line 251 after enrichment            | ✓ WIRED   | `enriched = aggregateMcpByName(enriched)` inserted between live-measurement and output branching. All 4 output paths see aggregated data. |
| `docs/JSON-SCHEMA.md`                                | `README.md` Machine-readable output section | `[JSON Schema](./docs/JSON-SCHEMA.md)`    | ✓ WIRED   | README line 166 links to the schema doc; docs/JSON-SCHEMA.md is discoverable from the project entry point.                           |
| All 4 subcommand files (`--json` arg)                | `docs/JSON-SCHEMA.md`                     | help text `'see docs/JSON-SCHEMA.md for schema'` | ✓ WIRED   | Users running `ccaudit <cmd> --help` see the schema doc referenced inline.                                                           |
| `packages/internal/package.json` (build: tsc)        | `packages/internal/tsconfig.json`         | composite + emitDeclarationOnly              | ✓ WIRED   | `tsc` invocation reuses the typecheck tsconfig; produces .d.ts declarations only; idempotent.                                        |
| `packages/terminal/package.json` (build: tsc)        | `packages/terminal/tsconfig.json`         | composite + emitDeclarationOnly              | ✓ WIRED   | Same pattern.                                                                                                                        |
| *(existing links from prior verification — all still WIRED)* | —                                 | —                                            | ✓ WIRED   | All 10 key links from 2026-04-04 verification (renderers → color.ts, commands → outputArgs, commands → _output-mode.ts, etc.) still verify. |

### Data-Flow Trace (Level 4)

| Artifact              | Data Variable                              | Source                                                                                                                         | Produces Real Data | Status      |
| --------------------- | ------------------------------------------ | ------------------------------------------------------------------------------------------------------------------------------ | ------------------ | ----------- |
| mcp.ts aggregated JSON | `enriched` → `aggregateMcpByName(enriched)` → `envelope.items[].projectPaths` | `scanMcp(allInvocations)` ← `parseSession()` ← `discoverSessionFiles()` with per-project dedup preserved | ✓ YES              | ✓ FLOWING   |
| mcp.ts aggregated CSV  | Same aggregated enriched                   | Same upstream                                                                                                                  | ✓ YES              | ✓ FLOWING   |
| mcp.ts aggregated TSV  | Same aggregated enriched                   | Same upstream                                                                                                                  | ✓ YES              | ✓ FLOWING   |
| docs/JSON-SCHEMA.md     | Schema text                                | Plan 06-07 Task 6 derived from D-16 + _output-mode.ts tests                                                                    | ✓ YES (static)     | ✓ FLOWING   |
| *(all other data flows from prior verification preserved)* | —                                 | —                                                                                                                              | ✓ YES              | ✓ FLOWING   |

End-to-end behavioral verification this session: `mcp --csv` produces zero duplicate rows (verified via `awk -F, '{print $1}' | sort | uniq -d | wc -l = 0`). `mcp --json` first item has `projectPaths` property (verified via jq `has("projectPaths") → true`). Full envelope still has `items`, `meta.timestamp`, `meta.exitCode` in camelCase.

### Behavioral Spot-Checks (Re-verification Round 3)

| #   | Behavior                                                                             | Command                                                                                    | Result                                                                                  | Status                                       |
| --- | ------------------------------------------------------------------------------------ | ------------------------------------------------------------------------------------------ | --------------------------------------------------------------------------------------- | -------------------------------------------- |
| 1   | `ccaudit ghost --help` lists output flags                                            | `node dist/index.js ghost --help`                                                          | Shows -q/--quiet, --csv, --ci, -s/--since, -j/--json, -v/--verbose, **--no-color**      | ✓ PASS                                       |
| 2   | `ccaudit ghost --ci` exits 1 when ghosts found                                       | `ghost --ci > /dev/null; echo $?`                                                          | EXIT=1                                                                                  | ✓ PASS (OUTP-01, OUTP-05)                    |
| 3   | `ccaudit trend --json` always exits 0                                                | `trend --json > /dev/null; echo $?`                                                        | EXIT=0                                                                                  | ✓ PASS (OUTP-01)                             |
| 4   | `ccaudit ghost --csv` produces RFC 4180 header + rows                                | `ghost --csv \| head -5`                                                                   | Header + real rows                                                                      | ✓ PASS (OUTP-07)                             |
| 5   | `NO_COLOR=1 ccaudit ghost` produces ANSI-free output                                 | `NO_COLOR=1 ghost --quiet \| cat -v \| grep -c '\^\['`                                    | 0 escape sequences                                                                      | ✓ PASS (OUTP-02)                             |
| 6   | `ccaudit ghost --quiet` produces TSV                                                 | `ghost --quiet \| cat -v`                                                                  | Tab-separated, no decoration                                                            | ✓ PASS (OUTP-03)                             |
| 7   | `ccaudit ghost --verbose` routes progress to stderr                                  | `ghost --verbose 2>&1 1>/dev/null`                                                         | Messages on stderr                                                                      | ✓ PASS (OUTP-04)                             |
| 8   | `ccaudit ghost --ci` includes D-16 meta envelope                                     | `ghost --ci \| jq '.meta.exitCode, (.items \| type)'`                                      | `1` + `"array"` — camelCase envelope preserved                                          | ✓ PASS (OUTP-06)                             |
| 9   | Full test suite passes                                                               | `pnpm exec vitest --run`                                                                   | **368 tests pass across 45 files** (up from 357 baseline, +11 new)                      | ✓ PASS                                       |
| 10  | `pnpm -r build` from workspace root                                                  | `pnpm -r build`                                                                            | EXIT=0, 3 packages build cleanly, 0 ERR_PNPM_RECURSIVE_RUN_NO_SCRIPT                    | ✓ PASS                                       |
| 11  | Coverage thresholds still enforced                                                   | `pnpm exec vitest --run --coverage`                                                        | 93.61/84.71/96/94.4 (stmts/branches/fns/lines) above 80/70/80/80                        | ✓ PASS                                       |
| 12  | `pnpm -r typecheck` all projects                                                     | `pnpm -r typecheck`                                                                        | EXIT=0                                                                                  | ✓ PASS                                       |
| 13  | `npm pack --dry-run` succeeds with fresh prepack                                     | `cd apps/ccaudit && npm pack --dry-run`                                                    | 72 kB tarball, zero runtime deps                                                        | ✓ PASS (OUTP-06)                             |
| 14  | Fresh dist/index.js reflects current source                                          | `head -1 dist/index.js; grep -c 'import.meta.vitest' dist/index.js`                        | `#!/usr/bin/env node`, 0 matches                                                        | ✓ PASS                                       |
| **15** | **`docs/JSON-SCHEMA.md` exists and documents camelCase envelope**                | `wc -l docs/JSON-SCHEMA.md; grep -c 'meta.exitCode' docs/JSON-SCHEMA.md`                   | **101 lines; field documented**                                                         | **✓ PASS (Gap #3 closed)**                  |
| **16** | **`README.md` contains 'Machine-readable output' section referencing schema doc**  | `grep -n 'JSON-SCHEMA.md\|Machine-readable output' README.md`                              | **Line 148 section header; line 166 link**                                              | **✓ PASS (Gap #3 closed)**                  |
| **17** | **`--no-color` appears in --help for root + 4 subcommands**                       | `node dist/index.js --help; for c in ghost inventory mcp trend; do node dist/index.js $c --help; done \| grep -c 'no-color'` | **5/5 (one per --help invocation)**                                                     | **✓ PASS (Gap #4 closed)**                  |
| **18** | **`mcp --csv` produces 0 duplicate rows**                                         | `mcp --csv \| tail -n +2 \| awk -F, '{print $1}' \| sort \| uniq -d \| wc -l`              | **0**                                                                                   | **✓ PASS (Gap #5 closed)**                  |
| **19** | **`mcp --json .items[0]` includes `projectPaths` field**                          | `mcp --json \| jq '.items[0] \| has("projectPaths")'`                                      | **`true`** (2 items, first has projectPaths)                                            | **✓ PASS (Gap #5 closed)**                  |
| **20** | **`pnpm -r build` exits 0 from workspace root AND subpackage dirs**               | `pnpm -r build` from root, `packages/internal`, `packages/terminal`                        | **3/3 EXIT=0, 0 ERR_PNPM_RECURSIVE_RUN_NO_SCRIPT**                                      | **✓ PASS (Gap #6 closed)**                  |
| **21** | **Three invariant files unchanged since plan start (88af4a4)**                    | `git diff 88af4a4..HEAD -- <3 files> \| wc -l`                                             | **_output-mode.ts: 0, color.ts: 0, scan-mcp.ts: 0**                                     | **✓ PASS (D-16 / D-07 / RMED-06 preserved)**|

**All 21 spot-checks pass.** Checks 15–21 are new additions from the 06-07 gap-closure re-verification. Checks 1–14 re-verify the original Phase 6 behaviors still hold after Plan 06-07's changes.

### Requirements Coverage

| Requirement | Source Plan(s)                              | Description                                                                     | Status      | Evidence                                                                                                                                                                                                           |
| ----------- | ------------------------------------------- | ------------------------------------------------------------------------------- | ----------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| OUTP-01     | 06-02, 06-04                                | Exit codes: 0 = no ghosts, 1 = ghosts found (CI/pre-commit use)                 | ✓ SATISFIED | Spot-checks #2, #3, #8. Unchanged by 06-07.                                                                                                                                                                        |
| OUTP-02     | 06-01, 06-04, **06-07**                     | `NO_COLOR` env respected; `--no-color` flag available                           | ✓ SATISFIED | Spot-check #5 (NO_COLOR env strips ANSI) + **spot-check #17 (new from 06-07: flag is visible in --help for root and all 4 subcommands)**. Initial D-07 runtime contract preserved per spot-check #21.            |
| OUTP-03     | 06-01, 06-02, 06-04                         | `--quiet` / `-q`: machine-readable data only                                    | ✓ SATISFIED | Spot-check #6. Unchanged by 06-07.                                                                                                                                                                                 |
| OUTP-04     | 06-02, 06-04                                | `--verbose` / `-v`: scan details                                                | ✓ SATISFIED | Spot-check #7. Unchanged by 06-07.                                                                                                                                                                                 |
| OUTP-05     | 06-02, 06-04                                | `--ci` flag: exit-code + quiet + JSON                                           | ✓ SATISFIED | Spot-check #2, #8. Unchanged by 06-07.                                                                                                                                                                             |
| OUTP-06     | 06-02, 06-03, 06-04, 06-05, 06-06, **06-07** | `--json` export on all read commands AND CI coverage threshold enforcement      | ✓ SATISFIED | Spot-checks #8, #11, #13. **Plus spot-checks #15, #16 (JSON schema now publicly documented via docs/JSON-SCHEMA.md + README link) and #19 (new `projectPaths` field for mcp traceability).** Discoverability gap from manual test now closed. |
| OUTP-07     | 06-01, 06-02, 06-03, 06-04, 06-05, **06-07** | `--csv` export on all read commands AND OS matrix                              | ✓ SATISFIED | Spot-check #4. **Plus spot-check #18 (new from 06-07: `mcp --csv` no longer emits cross-project duplicate rows — presentation-layer aggregation via aggregateMcpByName).**                                        |

**All 7 OUTP requirements are SATISFIED.** No orphaned requirements. Plan 06-07 strengthened evidence for OUTP-02, OUTP-06, and OUTP-07 by closing the four manual-test escape gaps.

### Anti-Patterns Found

| File                                             | Line(s)   | Pattern                                           | Severity    | Impact                                                                                                                                         |
| ------------------------------------------------ | --------- | ------------------------------------------------- | ----------- | ---------------------------------------------------------------------------------------------------------------------------------------------- |
| `packages/internal/src/token/mcp-live-client.ts` | 78-79, 106 | Hardcoded `"not available"` string in stderr      | ℹ️ Info    | NOT a stub — documented in Plan 06-02 summary as correct runtime behavior for non-stdio MCP transports.                                         |
| `packages/internal/src/token/estimate.ts` (unused `unlink` import at line 69) | 69 | Unused import | ⚠️ Warning | **Pre-existing, out of Plan 06-07 scope.** Logged in `deferred-items.md`. NOT a 06-07 regression — discovered because Task 8 added `pnpm lint` to the verification gate for the first time. Not a v1.0 blocker. |
| `packages/internal/src/scanner/scan-all.ts` (unused `InvocationSummary` import at line 5) | 5 | Unused import | ⚠️ Warning | Same as above — pre-existing, deferred. |
| `apps/ccaudit/scripts/gen-version.mjs` | whole file | ESLint tsconfig project-service discovery error | ⚠️ Warning | Same as above — pre-existing (committed as b962a77 in Phase 7), deferred. Requires eslint config change, not in 06-07 scope. |

**No blocker anti-patterns.** The 3 warnings are pre-existing lint drift discovered during Plan 06-07 Task 8 verification gate (which added `pnpm lint` for the first time). All are documented in `.planning/phases/06-output-control-polish/deferred-items.md` with rationale for deferral. They do not affect binary, tests, coverage, build, typecheck, publish, or any tester-visible behavior.

### Human Verification Required

None — all 21 testable behaviors were verified programmatically via behavioral spot-checks. The docs/JSON-SCHEMA.md artifact is static and verifiable by content check; the gap-closure fixes are all observable via command output or git diffs.

### Gaps Summary

**No gaps remain.** All 6 gaps from prior verifications are closed:

- **Gap #1** (CI coverage enforcement silent no-op) — Closed by Plan 06-05 (commits 6fd80d4, 08879b5, 26b80c4)
- **Gap #2** (commit e3dbe01 wiped scripts/devDependencies from apps/ccaudit/package.json) — Closed by Plan 06-06 (commit 0d4b5af)
- **Gap #3** (JSON schema contract undocumented) — Closed by Plan 06-07 Tasks 6+7 (commits 7c05727, 0d1b7ed)
- **Gap #4** (--no-color invisible in --help) — Closed by Plan 06-07 Tasks 2+5 (commits af75735, 79b0382, 379db85)
- **Gap #5** (mcp --csv cross-project duplicate rows) — Closed by Plan 06-07 Tasks 3+4 (commits 86fce53, 4da6e50)
- **Gap #6** (pnpm -r build from subpackage dirs) — Closed by Plan 06-07 Task 1 (commit 34a31af)

All 7 observable truths pass. All 7 OUTP requirements are satisfied. All 21 behavioral spot-checks pass. All 3 invariants (D-16 JSON envelope, D-07 initColor runtime, Phase 8 RMED-06 scanner per-project dedup) are preserved with 0-line git diffs since plan start (88af4a4).

**Phase 6 is ready to close.** The v1.0 launch candidate is production-ready: shared output control across all 4 CLI commands, exit code semantics, JSON meta envelope with public schema documentation, CSV export with cross-project MCP deduplication, verbose-to-stderr routing, quiet TSV output, CI matrix on ubuntu + macOS with genuinely-enforced 80% coverage thresholds, publishable npm package with complete metadata and zero runtime dependencies, AND `--no-color` discoverable in every `--help` output with canonical JSON schema reference at docs/JSON-SCHEMA.md.

---

*Verified: 2026-04-05T06:50:00Z*
*Verifier: Claude (gsd-verifier, third re-verification run after Plans 06-05, 06-06, and 06-07 gap closure)*

---

*Previous footer: Verified 2026-04-04T17:10:00Z (after Plans 06-05 + 06-06 closed Gaps #1/#2)*
*Original footer: Verified 2026-04-04T15:32:11Z (initial Phase 6 verification)*
*Escaped-gaps amendment: 2026-04-05T06:00:00Z (Gaps #3–#6 discovered during first end-to-end manual test)*
