---
phase: 06-output-control-polish
plan: 04
type: execute
wave: 3
depends_on: ["06-02"]
files_modified:
  - README.md
  - apps/ccaudit/package.json
autonomous: true
requirements: [OUTP-01, OUTP-02, OUTP-03, OUTP-04, OUTP-05, OUTP-06, OUTP-07]

must_haves:
  truths:
    - "README documents all new flags: --quiet/-q, --csv, --ci, --no-color, --verbose stderr behavior"
    - "README shows CI usage example with --ci flag and exit code semantics"
    - "npm pack --dry-run succeeds and shows zero runtime dependencies"
    - "package.json has keywords, repository, license, author, and homepage fields"
  artifacts:
    - path: "README.md"
      provides: "Publication-ready documentation for v1.0 launch"
      contains: "--ci"
    - path: "apps/ccaudit/package.json"
      provides: "npm metadata ready for publication"
      contains: "keywords"
  key_links:
    - from: "README.md"
      to: "apps/ccaudit/src/cli/commands/ghost.ts"
      via: "Documents CLI flags and exit code behavior"
      pattern: "--ci|--quiet|--csv|--no-color"
---

<objective>
Update README with Phase 6 flag documentation and CI usage examples, and finalize npm package metadata for v1.0 publication.

Purpose: ROADMAP SC-5 requires "README, npm metadata, and package are publication-ready (this is the v1.0 launch candidate)." This plan addresses that gap. It depends on Plan 02 (all flags are wired) so the documentation matches the actual implementation.
Output: Updated README.md with flag reference and CI examples, updated package.json with npm metadata, validated with npm pack --dry-run.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-output-control-polish/06-CONTEXT.md
@.planning/phases/06-output-control-polish/06-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update README with output control flags and CI usage, finalize npm metadata</name>
  <files>
    README.md
    apps/ccaudit/package.json
  </files>
  <read_first>
    README.md
    apps/ccaudit/package.json
  </read_first>
  <action>
    **README.md updates:**

    1. **Update the Usage section** to include the new output control flags alongside existing examples:
       ```bash
       npx ccaudit                     # ghost inventory, last 7 days
       npx ccaudit ghost --since 30d   # configurable threshold
       npx ccaudit ghost --json        # structured JSON with meta envelope
       npx ccaudit ghost --csv         # RFC 4180 CSV for spreadsheets
       npx ccaudit ghost --quiet       # machine-parseable TSV (pipe-friendly)
       npx ccaudit --no-color ghost    # ANSI-free output
       npx ccaudit inventory           # full inventory + all usage stats
       npx ccaudit mcp                 # MCP servers: token cost + frequency
       npx ccaudit mcp --live          # exact token count via live connection
       npx ccaudit trend               # invocation frequency over time
       npx ccaudit trend --csv         # time-series CSV export
       ```

    2. **Add a "CI / Scripting" section** after the Usage section (before Dry-run), with these subsections:

       **Exit codes:**
       - `ghost`, `inventory`, `mcp` commands exit 1 when ghosts are found, 0 otherwise
       - `trend` always exits 0 (informational data)

       **GitHub Actions example:**
       ```yaml
       - run: npx ccaudit@latest --ci
       ```
       Explain that `--ci` is shorthand for `--json --quiet` with exit codes. Show piping: `npx ccaudit --ci | jq .healthScore.score`.

       **Scripting examples:**
       ```bash
       # Count ghosts
       npx ccaudit ghost --quiet | wc -l

       # Export to CSV for Google Sheets
       npx ccaudit ghost --csv > ghosts.csv

       # Verbose scan progress on stderr, clean JSON on stdout
       npx ccaudit ghost --json --verbose 2>/dev/null > report.json
       ```

       **NO_COLOR support:**
       Mention that ccaudit respects the `NO_COLOR` environment variable (https://no-color.org/) and the `--no-color` flag. Either one disables all ANSI color codes.

    3. **Add a "Flags Reference" section** with a concise table:
       | Flag | Short | Description |
       |------|-------|-------------|
       | `--since <duration>` | `-s` | Time window (7d, 30d, 2w) |
       | `--json` | `-j` | JSON output with meta envelope |
       | `--csv` | | RFC 4180 CSV export |
       | `--quiet` | `-q` | Machine-readable only (TSV, compact JSON, headerless CSV) |
       | `--verbose` | `-v` | Scan details on stderr |
       | `--ci` | | CI mode: `--json --quiet` with exit codes |
       | `--no-color` | | Disable ANSI colors |
       | `--live` | | (mcp only) Live MCP server measurement |

    4. **Update the Status section** to reflect v1.0 readiness:
       Replace "Pre-release. Schema validation and PRD in progress. Not yet published to npm." with:
       "**v1.0** -- Analysis-only release. Ghost detection, token attribution, all output formats (JSON/CSV/TSV). CI-ready with exit codes."

    **apps/ccaudit/package.json updates:**

    Add missing npm metadata fields for publication readiness:
    ```json
    {
      "keywords": [
        "claude-code",
        "claude",
        "audit",
        "ghost",
        "mcp",
        "tokens",
        "cli",
        "npx"
      ],
      "license": "MIT",
      "repository": {
        "type": "git",
        "url": "https://github.com/helldrik/ccaudit"
      },
      "homepage": "https://github.com/helldrik/ccaudit#readme",
      "author": "Fabio D."
    }
    ```

    Note: The repository URL is a placeholder -- the executor should check if there is a git remote configured and use that URL instead: `git remote get-url origin`. If no remote exists, use a reasonable placeholder and leave a TODO comment.

    After edits, validate with:
    ```bash
    cd apps/ccaudit && npm pack --dry-run 2>&1
    ```
    Verify: zero `dependencies` in output (only `devDependencies`), `files` field limits to `dist/`, `bin` field is correct.
  </action>
  <verify>
    <automated>grep -c '\-\-ci' README.md && grep -c 'keywords' apps/ccaudit/package.json && cd apps/ccaudit && npm pack --dry-run 2>&1 | grep -c 'total files'</automated>
  </verify>
  <done>
    - README.md documents --quiet, --csv, --ci, --no-color, --verbose flags
    - README.md has CI/GitHub Actions usage example
    - README.md has scripting examples (wc -l, csv export, verbose + json)
    - README.md Status section updated to v1.0
    - README.md has Flags Reference table
    - apps/ccaudit/package.json has keywords, license, repository, homepage, author
    - npm pack --dry-run succeeds with zero runtime dependencies
  </done>
  <acceptance_criteria>
    - File README.md contains `--ci`
    - File README.md contains `--quiet`
    - File README.md contains `--csv`
    - File README.md contains `--no-color`
    - File README.md contains `NO_COLOR`
    - File README.md contains `exit 1` or `exit code` or `exits 1`
    - File README.md contains `GitHub Actions` or `CI`
    - File README.md contains `v1.0`
    - File apps/ccaudit/package.json contains `"keywords"`
    - File apps/ccaudit/package.json contains `"license"`
    - File apps/ccaudit/package.json contains `"repository"`
    - `cd apps/ccaudit && npm pack --dry-run` exits 0
  </acceptance_criteria>
</task>

</tasks>

<verification>
- README.md has at least one mention each of: --ci, --quiet, --csv, --no-color, NO_COLOR, exit code, GitHub Actions
- apps/ccaudit/package.json has keywords, license, repository fields
- `cd apps/ccaudit && npm pack --dry-run` succeeds and shows 0 runtime dependencies
- `grep -c '"dependencies"' apps/ccaudit/package.json` returns 0 (no runtime deps field)
</verification>

<success_criteria>
- README is publication-ready with all v1.0 flags documented (SC-5)
- npm metadata complete for npm publish (SC-5)
- npm pack validates package structure (SC-5)
- Zero runtime dependencies confirmed (DIST-02)
</success_criteria>

<output>
After completion, create `.planning/phases/06-output-control-polish/06-04-SUMMARY.md`
</output>
