---
phase: 06-output-control-polish
plan: 06
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/ccaudit/package.json
  - apps/ccaudit/dist/index.js
autonomous: true
gap_closure: true
requirements: [OUTP-06]

must_haves:
  truths:
    - "`pnpm -r build` includes apps/ccaudit in the scope (no ERR_PNPM_RECURSIVE_RUN_NO_SCRIPT for ccaudit)"
    - "`pnpm -r typecheck` includes apps/ccaudit in the scope"
    - "`pnpm -F ccaudit test` actually runs the 26 ccaudit test cases (not silent exit 0)"
    - "`pnpm -F ccaudit build` regenerates apps/ccaudit/dist/index.js with current source"
    - "The regenerated dist/index.js contains --quiet, --csv, --ci flags (proof it reflects post-06-02 source)"
    - "`npm pack --dry-run` in apps/ccaudit exits 0 AND publishes zero runtime dependencies"
    - "Workspace deps (@ccaudit/internal, @ccaudit/terminal) are re-linked via pnpm install"
    - "@vitest/coverage-v8 catalog reference (added by Plan 06-03, deleted by e3dbe01) is restored"
    - "The e3dbe01 additions (keywords, license, author, homepage, repository) are PRESERVED alongside the restored scripts/devDeps"
  artifacts:
    - path: "apps/ccaudit/package.json"
      provides: "Publication-ready package manifest with scripts, devDependencies, AND npm metadata"
      contains: "scripts"
      contains2: "devDependencies"
    - path: "apps/ccaudit/dist/index.js"
      provides: "Fresh build output reflecting current source (post-06-02 CLI wiring, post-06-04 metadata)"
      contains: "#!/usr/bin/env node"
  key_links:
    - from: "apps/ccaudit/package.json"
      to: "packages/internal/package.json"
      via: "devDependencies: { '@ccaudit/internal': 'workspace:*' }"
      pattern: "@ccaudit/internal.*workspace"
    - from: "apps/ccaudit/package.json"
      to: "packages/terminal/package.json"
      via: "devDependencies: { '@ccaudit/terminal': 'workspace:*' }"
      pattern: "@ccaudit/terminal.*workspace"
    - from: "apps/ccaudit/package.json"
      to: "pnpm-workspace.yaml catalog"
      via: "devDependencies uses 'catalog:' for all shared versions"
      pattern: "catalog:"
---

<objective>
Gap #2 closure: restore the `scripts` and `devDependencies` blocks in `apps/ccaudit/package.json` that were accidentally deleted by commit e3dbe01 (Plan 06-04), while preserving the npm metadata fields (keywords, license, author, homepage, repository) that commit correctly added.

Root cause: Commit e3dbe01 ("docs(06-04): document Phase 6 flags and finalize npm metadata") was supposed to add metadata fields while leaving the scripts and devDependencies blocks intact. Instead it shrank the file from 56 lines to 39 lines, wiping out the entire scripts block (build, test, typecheck, prepack) and the entire devDependencies block (all 10 entries: two workspace refs, seven catalog refs including the @vitest/coverage-v8 that Plan 06-03 had just added, and @types/node).

Impact on HEAD (all verified via VERIFICATION.md behavioral spot-check #10):
1. `pnpm -r build` silently skips apps/ccaudit with `ERR_PNPM_RECURSIVE_RUN_NO_SCRIPT None of the selected packages has a "build" script`. CI build job never builds the CLI binary.
2. `pnpm -r typecheck` skips apps/ccaudit.
3. `pnpm -F ccaudit test` silently exits 0 without running tests.
4. The `prepack` hook (`pnpm run build && clean-pkg-json`) never runs, meaning any future devDependencies additions would leak into the published package.
5. Workspace-internal imports (@ccaudit/internal, @ccaudit/terminal) are orphaned at the package.json level — pnpm's symlinks still exist on disk from before the regression but are no longer declared.
6. The @vitest/coverage-v8 catalog reference that Plan 06-03 added is GONE.
7. `npm pack --dry-run` appears to succeed ONLY because a stale `dist/index.js` from mtime 15:56 (predating both ec83269 Plan 06-02 and e3dbe01 Plan 06-04) happens to sit on disk. A fresh clone or CI run cannot reproduce this — the tarball would be empty/stale.

Fix: Restore the exact scripts and devDependencies blocks from commit `cb0932f` (the last commit before e3dbe01), while preserving every metadata field that e3dbe01 legitimately added. Re-run `pnpm install` to re-link workspace deps. Re-run `pnpm -F ccaudit build` to regenerate a fresh dist/index.js from current source.

Purpose: ROADMAP SC-5 ("README, npm metadata, and package are publication-ready") is currently a false claim. Phase 6 cannot be marked complete until apps/ccaudit is actually publishable — meaning the build pipeline runs, tests run, prepack strips devDeps, and the tarball contains fresh binary output reflecting the current source code.

Output: Restored apps/ccaudit/package.json with both the preserved metadata AND the restored scripts/devDependencies, plus a fresh dist/index.js regenerated from current source.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-output-control-polish/06-CONTEXT.md
@.planning/phases/06-output-control-polish/06-VERIFICATION.md
@.planning/phases/06-output-control-polish/06-03-PLAN.md
@.planning/phases/06-output-control-polish/06-04-PLAN.md

<interfaces>
<!-- Last-good state from commit cb0932f (verbatim output of `git show cb0932f:apps/ccaudit/package.json`) -->

```json
{
  "name": "ccaudit",
  "version": "0.0.1",
  "type": "module",
  "description": "Audit Claude Code ghost inventory — agents, skills, MCP servers, and memory files",
  "bin": {
    "ccaudit": "./src/index.ts"
  },
  "publishConfig": {
    "bin": {
      "ccaudit": "./dist/index.js"
    },
    "exports": {
      ".": "./dist/index.js"
    }
  },
  "files": ["dist"],
  "engines": {
    "node": ">=20.0.0"
  },
  "scripts": {
    "build": "tsdown",
    "test": "TZ=UTC vitest",
    "typecheck": "tsc --noEmit",
    "prepack": "pnpm run build && clean-pkg-json"
  },
  "devDependencies": {
    "@ccaudit/internal": "workspace:*",
    "@ccaudit/terminal": "workspace:*",
    "@vitest/coverage-v8": "catalog:",
    "gunshi": "catalog:",
    "valibot": "catalog:",
    "tsdown": "catalog:",
    "vitest": "catalog:",
    "clean-pkg-json": "catalog:",
    "typescript": "catalog:",
    "@types/node": "catalog:"
  }
}
```

<!-- Current HEAD state (after e3dbe01 regression) — e3dbe01 added these fields correctly AND deleted scripts/devDeps -->

```json
{
  "name": "ccaudit",
  "version": "0.0.1",
  "type": "module",
  "description": "Audit Claude Code ghost inventory — agents, skills, MCP servers, and memory files",
  "keywords": [
    "claude-code",
    "claude",
    "audit",
    "ghost",
    "mcp",
    "tokens",
    "cli",
    "npx"
  ],
  "license": "MIT",
  "author": "Fabio D.",
  "homepage": "https://github.com/0xD-Fabio/ccaudit#readme",
  "repository": {
    "type": "git",
    "url": "https://github.com/0xD-Fabio/ccaudit.git"
  },
  "bin": {
    "ccaudit": "./src/index.ts"
  },
  "publishConfig": {
    "bin": {
      "ccaudit": "./dist/index.js"
    },
    "exports": {
      ".": "./dist/index.js"
    }
  },
  "files": [
    "dist"
  ],
  "engines": {
    "node": ">=20.0.0"
  }
}
```

<!-- Target state (what this plan produces) = union of both: HEAD metadata fields + cb0932f scripts/devDeps -->
<!-- Field order: keep the HEAD order for the top section (name, version, type, description, keywords, license, author, homepage, repository, bin, publishConfig, files, engines), then append scripts + devDependencies from cb0932f. This is the minimum-diff approach. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Restore scripts and devDependencies blocks while preserving e3dbe01 metadata additions</name>
  <files>
    apps/ccaudit/package.json
  </files>
  <read_first>
    apps/ccaudit/package.json
    .planning/phases/06-output-control-polish/06-VERIFICATION.md
    .planning/phases/06-output-control-polish/06-03-PLAN.md
    .planning/phases/06-output-control-polish/06-04-PLAN.md
  </read_first>
  <action>
    Replace the entire contents of `apps/ccaudit/package.json` with the following JSON. This is a union of the current HEAD's metadata block and the cb0932f scripts + devDependencies blocks. Every character matters — do NOT paraphrase, reorder, or "improve" field values.

    ```json
    {
      "name": "ccaudit",
      "version": "0.0.1",
      "type": "module",
      "description": "Audit Claude Code ghost inventory — agents, skills, MCP servers, and memory files",
      "keywords": [
        "claude-code",
        "claude",
        "audit",
        "ghost",
        "mcp",
        "tokens",
        "cli",
        "npx"
      ],
      "license": "MIT",
      "author": "Fabio D.",
      "homepage": "https://github.com/0xD-Fabio/ccaudit#readme",
      "repository": {
        "type": "git",
        "url": "https://github.com/0xD-Fabio/ccaudit.git"
      },
      "bin": {
        "ccaudit": "./src/index.ts"
      },
      "publishConfig": {
        "bin": {
          "ccaudit": "./dist/index.js"
        },
        "exports": {
          ".": "./dist/index.js"
        }
      },
      "files": [
        "dist"
      ],
      "engines": {
        "node": ">=20.0.0"
      },
      "scripts": {
        "build": "tsdown",
        "test": "TZ=UTC vitest",
        "typecheck": "tsc --noEmit",
        "prepack": "pnpm run build && clean-pkg-json"
      },
      "devDependencies": {
        "@ccaudit/internal": "workspace:*",
        "@ccaudit/terminal": "workspace:*",
        "@vitest/coverage-v8": "catalog:",
        "gunshi": "catalog:",
        "valibot": "catalog:",
        "tsdown": "catalog:",
        "vitest": "catalog:",
        "clean-pkg-json": "catalog:",
        "typescript": "catalog:",
        "@types/node": "catalog:"
      }
    }
    ```

    Source-of-truth for the scripts + devDependencies blocks: `git show cb0932f:apps/ccaudit/package.json` (the last commit before e3dbe01 regression). Do not invent or substitute any entry — the devDependencies list is exactly 10 entries in the order shown above.

    Source-of-truth for the keywords/license/author/homepage/repository/description: HEAD `apps/ccaudit/package.json` (what e3dbe01 correctly added). The homepage and repository URLs are `https://github.com/0xD-Fabio/ccaudit...` which is the current git origin remote — do NOT change this to any other value. The e3dbe01 plan file (06-04-PLAN.md) had a placeholder "helldrik/ccaudit" URL but the executor correctly resolved it via `git remote get-url origin` to `0xD-Fabio/ccaudit`; preserve the resolved value.

    Top-level fields in target order: name, version, type, description, keywords, license, author, homepage, repository, bin, publishConfig, files, engines, scripts, devDependencies. This field order produces the smallest diff against HEAD (metadata block on top as HEAD already has, then the restored blocks appended).

    After writing, verify the file parses as valid JSON:
    ```bash
    node -e "const p = JSON.parse(require('fs').readFileSync('apps/ccaudit/package.json', 'utf-8')); console.log('scripts:', Object.keys(p.scripts || {})); console.log('devDeps:', Object.keys(p.devDependencies || {}).length);"
    ```
    Expected output:
    ```
    scripts: [ 'build', 'test', 'typecheck', 'prepack' ]
    devDeps: 10
    ```
  </action>
  <verify>
    <automated>node -e "const p = JSON.parse(require('fs').readFileSync('apps/ccaudit/package.json', 'utf-8')); if (!p.scripts || !p.scripts.build) process.exit(1); if (!p.devDependencies || Object.keys(p.devDependencies).length !== 10) process.exit(2); if (!p.keywords || !p.license || !p.repository) process.exit(3);"</automated>
  </verify>
  <done>
    - apps/ccaudit/package.json contains `scripts` block with 4 keys: build, test, typecheck, prepack
    - apps/ccaudit/package.json contains `devDependencies` block with exactly 10 keys
    - apps/ccaudit/package.json preserves keywords, license, author, homepage, repository from e3dbe01
    - JSON parses without errors
    - homepage and repository URLs reference 0xD-Fabio/ccaudit (the actual git remote)
  </done>
  <acceptance_criteria>
    - `cd apps/ccaudit && grep -q '"build": "tsdown"' package.json` exits 0
    - `cd apps/ccaudit && grep -q '"test": "TZ=UTC vitest"' package.json` exits 0
    - `cd apps/ccaudit && grep -q '"typecheck": "tsc --noEmit"' package.json` exits 0
    - `cd apps/ccaudit && grep -q '"prepack": "pnpm run build && clean-pkg-json"' package.json` exits 0
    - `cd apps/ccaudit && grep -q '"@ccaudit/internal": "workspace:\*"' package.json` exits 0
    - `cd apps/ccaudit && grep -q '"@ccaudit/terminal": "workspace:\*"' package.json` exits 0
    - `cd apps/ccaudit && grep -q '"@vitest/coverage-v8": "catalog:"' package.json` exits 0
    - `cd apps/ccaudit && grep -q '"gunshi": "catalog:"' package.json` exits 0
    - `cd apps/ccaudit && grep -q '"valibot": "catalog:"' package.json` exits 0
    - `cd apps/ccaudit && grep -q '"tsdown": "catalog:"' package.json` exits 0
    - `cd apps/ccaudit && grep -q '"vitest": "catalog:"' package.json` exits 0
    - `cd apps/ccaudit && grep -q '"clean-pkg-json": "catalog:"' package.json` exits 0
    - `cd apps/ccaudit && grep -q '"typescript": "catalog:"' package.json` exits 0
    - `cd apps/ccaudit && grep -q '"@types/node": "catalog:"' package.json` exits 0
    - `cd apps/ccaudit && grep -q '"keywords"' package.json` exits 0
    - `cd apps/ccaudit && grep -q '"license": "MIT"' package.json` exits 0
    - `cd apps/ccaudit && grep -q '"author": "Fabio D."' package.json` exits 0
    - `cd apps/ccaudit && grep -q '0xD-Fabio/ccaudit' package.json` exits 0
    - `node -e "JSON.parse(require('fs').readFileSync('apps/ccaudit/package.json','utf-8'))"` exits 0 (valid JSON)
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Re-link workspace deps via pnpm install and regenerate dist/ via pnpm -F ccaudit build</name>
  <files>
    apps/ccaudit/dist/index.js
  </files>
  <read_first>
    apps/ccaudit/package.json
    apps/ccaudit/tsdown.config.ts
    apps/ccaudit/src/index.ts
    apps/ccaudit/src/cli/commands/ghost.ts
    .planning/phases/06-output-control-polish/06-VERIFICATION.md
  </read_first>
  <action>
    After Task 1 restored the package.json, pnpm now knows apps/ccaudit has devDependencies to link. Run:

    ```bash
    pnpm install
    ```

    Expected: pnpm re-links @ccaudit/internal and @ccaudit/terminal into apps/ccaudit/node_modules (these are workspace symlinks). No new packages should need to be downloaded because pnpm-workspace.yaml catalog entries already exist and packages/internal, packages/terminal, and the root devDeps haven't changed. Exit code 0.

    Then delete the stale dist/ and regenerate it from current source:

    ```bash
    rm -rf apps/ccaudit/dist
    pnpm -F ccaudit build
    ```

    Expected:
    - `pnpm -F ccaudit build` invokes `tsdown` (the script we restored in Task 1).
    - tsdown reads `apps/ccaudit/tsdown.config.ts`, bundles apps/ccaudit/src/index.ts along with its imports (including workspace packages @ccaudit/internal and @ccaudit/terminal), strips import.meta.vitest in-source tests per the top-level `define`, and writes `apps/ccaudit/dist/index.js`.
    - The dist file contains a shebang on line 1 (`#!/usr/bin/env node`).
    - The dist file reflects the CURRENT source: it must contain the `--quiet`, `--csv`, `--ci` flags that Plan 06-02 wired in commit ec83269. Prior to this rebuild the dist file was from 15:56 which predates ec83269.
    - Exit code 0.

    After build completes, verify the binary executes end-to-end:
    ```bash
    node apps/ccaudit/dist/index.js --help
    node apps/ccaudit/dist/index.js ghost --help
    ```
    Expected: help output lists `--quiet`, `--csv`, `--ci` alongside `--since`, `--json`, `--verbose`.

    Verify the shebang:
    ```bash
    head -1 apps/ccaudit/dist/index.js
    ```
    Expected: `#!/usr/bin/env node`

    Verify in-source tests stripped from dist:
    ```bash
    grep -c 'import.meta.vitest' apps/ccaudit/dist/index.js
    ```
    Expected: 0 (zero matches — in-source tests are stripped at build time).
  </action>
  <verify>
    <automated>pnpm install && rm -rf apps/ccaudit/dist && pnpm -F ccaudit build && test -f apps/ccaudit/dist/index.js && head -1 apps/ccaudit/dist/index.js | grep -q '#!/usr/bin/env node' && grep -q '\-\-quiet' apps/ccaudit/dist/index.js && grep -q '\-\-csv' apps/ccaudit/dist/index.js && grep -q '\-\-ci' apps/ccaudit/dist/index.js && ! grep -q 'import.meta.vitest' apps/ccaudit/dist/index.js</automated>
  </verify>
  <done>
    - `pnpm install` exits 0 (workspace deps re-linked)
    - `pnpm -F ccaudit build` exits 0 (tsdown bundles successfully)
    - apps/ccaudit/dist/index.js exists and is fresh (not the stale 15:56 artifact)
    - dist/index.js has `#!/usr/bin/env node` shebang on line 1
    - dist/index.js contains `--quiet`, `--csv`, `--ci` flag strings (proof it reflects post-06-02 source)
    - dist/index.js does NOT contain `import.meta.vitest` (in-source tests stripped)
    - `node dist/index.js --help` and `node dist/index.js ghost --help` execute without error
  </done>
  <acceptance_criteria>
    - `pnpm install` exits 0
    - `pnpm -F ccaudit build` exits 0
    - `test -f apps/ccaudit/dist/index.js` exits 0
    - `head -1 apps/ccaudit/dist/index.js | grep -q '#!/usr/bin/env node'` exits 0
    - `grep -q '\-\-quiet' apps/ccaudit/dist/index.js` exits 0 (flag is in the new build)
    - `grep -q '\-\-csv' apps/ccaudit/dist/index.js` exits 0
    - `grep -q '\-\-ci' apps/ccaudit/dist/index.js` exits 0
    - `grep -q 'import.meta.vitest' apps/ccaudit/dist/index.js` exits 1 (pattern NOT found, in-source tests stripped)
    - `node apps/ccaudit/dist/index.js --help` exits 0
    - `node apps/ccaudit/dist/index.js ghost --help 2>&1 | grep -q 'quiet'` exits 0
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Verify pnpm -r build, pnpm -r typecheck, pnpm -F ccaudit test, and npm pack --dry-run all succeed</name>
  <files>
  </files>
  <read_first>
    apps/ccaudit/package.json
    .planning/phases/06-output-control-polish/06-VERIFICATION.md
  </read_first>
  <action>
    This task is pure verification — no file modifications. It proves all the broken behaviors documented in VERIFICATION.md Behavioral Spot-Check #10 and Gap #2 are resolved.

    **Check 1: `pnpm -r build` no longer skips apps/ccaudit**
    ```bash
    pnpm -r build 2>&1 | tee /tmp/ccaudit-pr-build.log
    echo "EXIT=$?"
    ```
    Expected:
    - Exit code 0.
    - Output does NOT contain `ERR_PNPM_RECURSIVE_RUN_NO_SCRIPT`.
    - Output does NOT contain `None of the selected packages has a "build" script`.
    - Output shows build invocation for ccaudit (e.g. `apps/ccaudit build$ tsdown` or similar).
    - If the previous dist/ was regenerated in Task 2, tsdown may report "nothing to do" or rebuild — either is acceptable as long as the exit code is 0 and no skip error appears.

    **Check 2: `pnpm -r typecheck` no longer skips apps/ccaudit**
    ```bash
    pnpm -r typecheck 2>&1 | tee /tmp/ccaudit-pr-typecheck.log
    echo "EXIT=$?"
    ```
    Expected:
    - Exit code 0.
    - Output does NOT contain `ERR_PNPM_RECURSIVE_RUN_NO_SCRIPT`.
    - Output shows typecheck invocation for ccaudit (`apps/ccaudit typecheck$ tsc --noEmit`).

    **Check 3: `pnpm -F ccaudit test` actually runs ccaudit tests**
    ```bash
    pnpm -F ccaudit test -- --run 2>&1 | tee /tmp/ccaudit-pf-test.log
    echo "EXIT=$?"
    ```
    Expected:
    - Exit code 0.
    - Output contains a test run summary mentioning passing tests (e.g. `Tests  26 passed` or similar — the count is whatever apps/ccaudit currently has; VERIFICATION.md mentions 26 ccaudit tests).
    - Output does NOT show "0 tests" / "No tests found" / silent exit with no test output.

    **Check 4: `npm pack --dry-run` succeeds and shows zero runtime dependencies**
    ```bash
    cd apps/ccaudit && npm pack --dry-run 2>&1 | tee /tmp/ccaudit-npm-pack.log
    echo "EXIT=$?"
    cd - > /dev/null
    ```
    Expected:
    - Exit code 0.
    - Output contains the line `npm notice Tarball Details`.
    - `clean-pkg-json` runs as part of prepack and strips devDependencies before packing (devDependencies should not appear in the packed package.json view).
    - Output shows `dist/index.js` in the file list (proof the fresh build from Task 2 is what ships).
    - Output does NOT show any runtime `dependencies` (only `devDependencies` which get stripped, or nothing at all).

    Inspect the packed tarball's package.json to confirm zero runtime deps:
    ```bash
    cd apps/ccaudit
    npm pack --dry-run 2>&1 | grep -E "dependencies|dist/index.js" | head -20
    cd - > /dev/null
    ```

    **Check 5: `pnpm -r test -- --run` passes the full monorepo suite**
    ```bash
    pnpm -r test -- --run 2>&1 | tail -20
    echo "EXIT=$?"
    ```
    Expected: exit 0, all tests across internal + terminal + ccaudit pass.

    Collect the exit codes of all 5 checks. If any check fails, STOP and investigate — do NOT mark the task done. The expected end state is: all 5 checks pass, gap closure is confirmed empirically.
  </action>
  <verify>
    <automated>pnpm -r build 2>&1 | tee /tmp/ccaudit-verify-build.log && ! grep -q "ERR_PNPM_RECURSIVE_RUN_NO_SCRIPT" /tmp/ccaudit-verify-build.log && pnpm -r typecheck 2>&1 | tee /tmp/ccaudit-verify-typecheck.log && ! grep -q "ERR_PNPM_RECURSIVE_RUN_NO_SCRIPT" /tmp/ccaudit-verify-typecheck.log && pnpm -F ccaudit test -- --run 2>&1 | tee /tmp/ccaudit-verify-test.log && grep -q "passed" /tmp/ccaudit-verify-test.log && (cd apps/ccaudit && npm pack --dry-run 2>&1 | tee /tmp/ccaudit-verify-pack.log) && grep -q "dist/index.js" /tmp/ccaudit-verify-pack.log && pnpm -r test -- --run 2>&1 | tail -5</automated>
  </verify>
  <done>
    - `pnpm -r build` exits 0 and output does not contain ERR_PNPM_RECURSIVE_RUN_NO_SCRIPT
    - `pnpm -r typecheck` exits 0 and output does not contain ERR_PNPM_RECURSIVE_RUN_NO_SCRIPT
    - `pnpm -F ccaudit test -- --run` exits 0 with non-zero passing test count
    - `cd apps/ccaudit && npm pack --dry-run` exits 0 and lists dist/index.js
    - Packed package.json shows zero runtime dependencies (clean-pkg-json stripped devDeps)
    - `pnpm -r test -- --run` exits 0 (full monorepo suite)
    - All 5 behavioral spot-checks from VERIFICATION.md Behavioral Spot-Check #10 now pass
  </done>
  <acceptance_criteria>
    - `pnpm -r build 2>&1 | grep -c "ERR_PNPM_RECURSIVE_RUN_NO_SCRIPT"` returns 0
    - `pnpm -r build 2>&1 | grep -c "None of the selected packages has a \"build\" script"` returns 0
    - `pnpm -r typecheck 2>&1 | grep -c "ERR_PNPM_RECURSIVE_RUN_NO_SCRIPT"` returns 0
    - `pnpm -F ccaudit test -- --run 2>&1 | grep -Ec "(Tests|passed)"` returns a number > 0 (tests actually ran)
    - `cd apps/ccaudit && npm pack --dry-run` exits 0
    - `cd apps/ccaudit && npm pack --dry-run 2>&1 | grep -q "dist/index.js"` exits 0
    - `pnpm -r test -- --run` exits 0
  </acceptance_criteria>
</task>

</tasks>

<verification>
- `cd apps/ccaudit && node -e "const p = require('./package.json'); console.log(Object.keys(p.scripts).length, Object.keys(p.devDependencies).length)"` prints `4 10`
- `pnpm install` exits 0 (workspace deps re-linked after package.json restoration)
- `pnpm -F ccaudit build` exits 0 (tsdown runs successfully)
- `head -1 apps/ccaudit/dist/index.js` prints `#!/usr/bin/env node`
- `grep -q '\-\-quiet' apps/ccaudit/dist/index.js` exits 0 (fresh build includes Plan 06-02 flags)
- `grep -q 'import.meta.vitest' apps/ccaudit/dist/index.js` exits 1 (in-source tests stripped)
- `pnpm -r build 2>&1 | grep -c ERR_PNPM_RECURSIVE_RUN_NO_SCRIPT` returns 0 (no more skip error)
- `pnpm -r typecheck 2>&1 | grep -c ERR_PNPM_RECURSIVE_RUN_NO_SCRIPT` returns 0
- `pnpm -F ccaudit test -- --run 2>&1 | grep -Ec "(Tests|passed)"` returns > 0 (tests actually run, not silent exit)
- `cd apps/ccaudit && npm pack --dry-run` exits 0 and output lists dist/index.js
- `pnpm -r test -- --run` exits 0 (full monorepo suite, no regressions)
</verification>

<success_criteria>
- Gap #2 closed: ROADMAP SC-5 ("package is publication-ready") is actually true, not a false claim propped up by a stale dist artifact
- Scripts block fully restored (4 scripts: build, test, typecheck, prepack) per cb0932f source of truth
- devDependencies block fully restored (10 entries: 2 workspace refs + 7 catalog refs + @types/node) per cb0932f source of truth
- e3dbe01 metadata additions preserved (keywords, license, author, homepage, repository) — no data loss from the legitimate parts of that commit
- @vitest/coverage-v8 catalog reference that Plan 06-03 added is restored (required by Plan 06-05 for coverage invocation)
- Fresh dist/index.js regenerated from current source (includes --quiet, --csv, --ci flags from Plan 06-02)
- `pnpm -r build`, `pnpm -r typecheck`, `pnpm -F ccaudit test` all stop silently skipping apps/ccaudit
- `npm pack --dry-run` succeeds on a real build artifact (not the stale 15:56 leftover)
- Publication readiness is empirically verified, not assumed
</success_criteria>

<output>
After completion, create `.planning/phases/06-output-control-polish/06-06-SUMMARY.md`
</output>
