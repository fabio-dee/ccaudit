---
phase: 06-output-control-polish
plan: 07
id: 06-07
type: execute
wave: 1
depends_on: [06-01, 06-02, 06-04]
files_modified:
  - apps/ccaudit/src/cli/_shared-args.ts
  - apps/ccaudit/src/cli/commands/mcp.ts
  - apps/ccaudit/src/cli/commands/ghost.ts
  - apps/ccaudit/src/cli/commands/inventory.ts
  - apps/ccaudit/src/cli/commands/trend.ts
  - packages/internal/package.json
  - packages/terminal/package.json
  - docs/JSON-SCHEMA.md
  - docs/manual-test-results.md
  - README.md
  - apps/ccaudit/src/__tests__/help-output.test.ts
  - apps/ccaudit/src/__tests__/mcp-aggregation.test.ts
autonomous: true
gap_closure: true
requirements: [OUTP-02, OUTP-06, OUTP-07]

must_haves:
  truths:
    - "`docs/JSON-SCHEMA.md` exists and documents the D-16 camelCase envelope (items, meta.timestamp, meta.exitCode) with jq examples for all 4 commands"
    - "`README.md` contains a 'Machine-readable output' section that references `docs/JSON-SCHEMA.md`"
    - "`node apps/ccaudit/dist/index.js --help` lists `--no-color` in its options"
    - "`node apps/ccaudit/dist/index.js <cmd> --help` lists `--no-color` for every subcommand: ghost, inventory, mcp, trend"
    - "`node apps/ccaudit/dist/index.js <cmd> --help` mentions `JSON-SCHEMA.md` in the `--json` flag description for every subcommand"
    - "`ccaudit mcp --csv` produces no duplicate rows when the same MCP server is declared in multiple project-level .mcp.json files (fixture-based regression test passes)"
    - "`ccaudit mcp --json` items expose a new `projectPaths: string[]` field for traceability after aggregation"
    - "`pnpm -r build` exits 0 from workspace root AND from inside packages/internal and packages/terminal subpackage dirs (no ERR_PNPM_RECURSIVE_RUN_NO_SCRIPT)"
    - "`pnpm exec vitest --run` reports >= 357 tests passing (357 baseline preserved + new tests from Tasks 2/3/4/5)"
    - "Coverage thresholds still met: statements 80, branches 70, functions 80, lines 80 (per Phase 6 vitest.config.ts)"
    - "D-16 JSON envelope is NOT modified: `_output-mode.ts` buildJsonEnvelope still emits `items`, `meta.timestamp`, `meta.exitCode` (no renames); the locked test at _output-mode.ts:120-145 still passes unchanged"
    - "D-07 runtime color contract preserved: `packages/terminal/src/color.ts` initColor() still reads `process.argv.includes('--no-color')` as authoritative source (additive gunshi declaration only adds help metadata)"
    - "Scanner-layer dedup preserved: `packages/internal/src/scanner/scan-mcp.ts` per-project (projectPath::serverName) Set is unchanged (Phase 8 RMED-06 still knows per-project config keys)"
    - "docs/manual-test-results.md is corrected so future testers see the actual camelCase schema, with the three 'FAIL' entries (sections #2, #3, #4 of the errors list) reframed as spec-clarifications against D-16"
  artifacts:
    - path: "docs/JSON-SCHEMA.md"
      provides: "Canonical JSON envelope reference: meta.command/version/since/timestamp/exitCode + per-command payload key (items for ghost/inventory/mcp, buckets for trend) + jq examples"
      contains: "meta.exitCode"
    - path: "README.md"
      provides: "Machine-readable output section linking to docs/JSON-SCHEMA.md"
      contains: "JSON-SCHEMA.md"
    - path: "apps/ccaudit/src/cli/_shared-args.ts"
      provides: "outputArgs now declares 'no-color' so gunshi prints it in --help; flipped in-source test asserts key IS present; header comment updated"
      contains: "'no-color'"
    - path: "apps/ccaudit/src/cli/commands/mcp.ts"
      provides: "aggregateMcpByName helper + call site before output branching + projectPaths: string[] in JSON output + in-source tests"
      contains: "aggregateMcpByName"
    - path: "apps/ccaudit/src/cli/commands/ghost.ts"
      provides: "--json arg description extended to reference docs/JSON-SCHEMA.md"
      contains: "JSON-SCHEMA.md"
    - path: "apps/ccaudit/src/cli/commands/inventory.ts"
      provides: "Same --json description extension"
      contains: "JSON-SCHEMA.md"
    - path: "apps/ccaudit/src/cli/commands/trend.ts"
      provides: "Same --json description extension"
      contains: "JSON-SCHEMA.md"
    - path: "packages/internal/package.json"
      provides: "'build': 'tsc' script stub for pnpm -r build symmetry (idempotent via composite + emitDeclarationOnly)"
      contains: "\"build\": \"tsc\""
    - path: "packages/terminal/package.json"
      provides: "'build': 'tsc' script stub for pnpm -r build symmetry"
      contains: "\"build\": \"tsc\""
    - path: "apps/ccaudit/src/__tests__/help-output.test.ts"
      provides: "Integration regression test: spawns dist/index.js --help and <cmd> --help, greps for 'no-color'"
      contains: "no-color"
    - path: "apps/ccaudit/src/__tests__/mcp-aggregation.test.ts"
      provides: "End-to-end regression test: builds fixture with same MCP server in two .mcp.json files, asserts mcp --csv emits 1 row not 2"
      contains: "aggregateMcpByName"
    - path: "docs/manual-test-results.md"
      provides: "Corrected error entries for sections #2/#3/#4 with strike-through + 'Spec clarifications applied' reframing pointing to docs/JSON-SCHEMA.md"
      contains: "JSON-SCHEMA.md"
  key_links:
    - from: "apps/ccaudit/src/cli/_shared-args.ts"
      to: "apps/ccaudit/src/cli/commands/*.ts"
      via: "spread ...outputArgs in every command's args block (existing, unchanged)"
      pattern: "\\.\\.\\.outputArgs"
    - from: "apps/ccaudit/src/cli/commands/mcp.ts"
      to: "aggregateMcpByName helper (new, inline)"
      via: "enriched = aggregateMcpByName(enriched) call between enrichScanResults and output branching"
      pattern: "aggregateMcpByName\\(enriched\\)"
    - from: "docs/JSON-SCHEMA.md"
      to: "README.md Machine-readable output section"
      via: "relative link: [JSON schema](./docs/JSON-SCHEMA.md)"
      pattern: "JSON-SCHEMA\\.md"
    - from: "packages/internal/package.json"
      to: "packages/internal/tsconfig.json"
      via: "'build': 'tsc' reuses composite+emitDeclarationOnly config"
      pattern: "\"build\": \"tsc\""
    - from: "packages/terminal/package.json"
      to: "packages/terminal/tsconfig.json"
      via: "'build': 'tsc' reuses composite+emitDeclarationOnly config"
      pattern: "\"build\": \"tsc\""
---

<objective>
Close the four escaped gaps surfaced by the 2026-04-05 end-to-end manual test of the Phase 6 binary against a live `~/.claude/` environment. Phase 6 VERIFICATION.md was amended from `passed` to `gaps_found` after 15 test sections were run; 12 passed but 4 escaped gaps remain:

- **Gap #3 (Category A in the approved plan)** — JSON schema contract is undocumented. The code is CORRECT per Phase 6 D-16 (camelCase `items` / `meta.timestamp` / `meta.exitCode`). The tester expected a REST-API snake_case (`.data` / `generated_at` / `exit_code`) shape with no public documentation to correct that assumption. The vitest suite LOCKS the current contract at `apps/ccaudit/src/cli/_output-mode.ts:120-145`. Fix is documentation-only: create `docs/JSON-SCHEMA.md`, link from README, extend each command's `--json` flag description, correct `docs/manual-test-results.md`. NO CODE RENAMES — D-16 is frozen.

- **Gap #4 (Category B1)** — `--no-color` is invisible in every `--help` output. D-07 intentionally made `initColor()` read `process.argv` directly so the flag works at any position, but the side effect is that gunshi never sees `--no-color` as a declared arg, so it's absent from help metadata. Fix: add `'no-color'` to `outputArgs` so gunshi declares it on every subcommand; keep `initColor()` unchanged (still the authoritative runtime source since both paths parse the same `process.argv`). Flip the in-source test at `_shared-args.ts:64-67` which currently asserts the key is NOT present.

- **Gap #5 (Category B2)** — `ccaudit mcp --csv` and the rendered MCP table emit duplicate rows when the same MCP server is declared in multiple project-level `.mcp.json` files. The scanner per-project dedup (`(projectPath::serverName)`) is intentional for Phase 8 `RMED-06` (needs per-project config keys to rewrite). Fix is presentation-layer aggregation in `commands/mcp.ts`: new `aggregateMcpByName(enriched)` helper groups by server name, merges tier (least-ghost wins), takes max lastUsed, sums invocationCount, adds `projectPaths: string[]` to JSON output for traceability. Scanner and `mcp-table.ts` unchanged.

- **Gap #6 (Category B3)** — `pnpm -r build` errors with `ERR_PNPM_RECURSIVE_RUN_NO_SCRIPT` from subpackage directories because `packages/internal` and `packages/terminal` lack a `build` script. CI is green only because pnpm tolerates partial matches when invoked from workspace root. Fix: add `"build": "tsc"` stubs to both packages (symmetric with existing typecheck, idempotent via composite+emitDeclarationOnly). Verified: both tsconfig.json files already have `composite: true` + `emitDeclarationOnly: true` so `tsc` is safe and produces declarations.

This is a surgical gap-closure plan translated verbatim from the user-approved plan at `/Users/helldrik/.claude/plans/stateless-watching-koala.md`, which was approved via ExitPlanMode after parallel three-angle investigation. Every fix, file location, test addition, and verification command in that document is preserved here in GSD task format.

Scope is strictly limited to the 12 files in `files_modified`. No refactoring. No touching the JSON envelope code. No Phase 8 work. No renaming of locked fields.

Output:
- `docs/JSON-SCHEMA.md` (new, ≤80 lines) documenting the frozen D-16 envelope
- `README.md` updated with a Machine-readable output section
- `--no-color` visible in `--help` for root command and all 4 subcommands
- `ccaudit mcp --csv` and table no longer emit duplicate rows cross-project
- `mcp --json` items include `projectPaths: string[]` for traceability
- `pnpm -r build` succeeds from any directory in the monorepo
- 357 baseline tests still pass + new regression tests from Tasks 2/3/4/5
- Coverage gate still at Phase 6 thresholds (80/70/80/80)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-output-control-polish/06-CONTEXT.md
@.planning/phases/06-output-control-polish/06-VERIFICATION.md
@.planning/phases/06-output-control-polish/06-06-PLAN.md

<interfaces>
<!-- Key contracts the executor needs to implement these fixes without codebase exploration. -->
<!-- All snippets below are verbatim extracts from files currently on disk (2026-04-05). -->

**D-16 JSON envelope contract (FROZEN — locked by vitest at _output-mode.ts:120-145):**
From `apps/ccaudit/src/cli/_output-mode.ts:60-76`:
```typescript
export function buildJsonEnvelope<T extends Record<string, unknown>>(
  command: string,
  since: string,
  exitCode: number,
  data: T,
): Record<string, unknown> {
  return {
    meta: {
      command,
      version: '0.0.1',
      since,
      timestamp: new Date().toISOString(),
      exitCode,
    },
    ...data,
  };
}
```

Locked test at lines 120-145:
```typescript
describe('buildJsonEnvelope', () => {
  it('wraps data with meta envelope', () => {
    const data = { items: [1, 2, 3] };
    const envelope = buildJsonEnvelope('ghost', '7d', 1, data);
    expect(envelope).toHaveProperty('meta');
    expect(envelope).toHaveProperty('items');
    const meta = envelope.meta as Record<string, unknown>;
    expect(meta.command).toBe('ghost');
    expect(meta.since).toBe('7d');
    expect(meta.exitCode).toBe(1);
  });
  // ... checks typeof meta.version is string
  // ... checks meta.timestamp is ISO 8601
});
```

**Current _shared-args.ts (the target of the Gap #4 fix):**
```typescript
/**
 * Shared CLI flag definitions for all subcommands.
 *
 * These flags are spread into every command's `args` object.
 * IMPORTANT: --no-color is NOT here (per D-07). It is detected at root level
 * by initColor() reading process.argv directly. This means both
 * `ccaudit --no-color ghost` and `ccaudit ghost --no-color` work.
 */
export const outputArgs = {
  quiet: { type: 'boolean' as const, short: 'q', description: '...', default: false },
  csv:   { type: 'boolean' as const, description: 'Output as CSV (RFC 4180)', default: false },
  ci:    { type: 'boolean' as const, description: 'CI mode: --json --quiet with exit codes (implies --json --quiet)', default: false },
} as const;

// ... in-source tests include at lines 64-67:
it('does NOT have no-color key', () => {
  expect(outputArgs).not.toHaveProperty('no-color');
  expect(outputArgs).not.toHaveProperty('noColor');
});
```

**Current color.ts D-07 contract (MUST NOT CHANGE):**
From `packages/terminal/src/color.ts:19-23`:
```typescript
export function initColor(): void {
  const argvHasNoColor = process.argv.includes('--no-color');
  const envHasNoColor = typeof process.env.NO_COLOR === 'string' && process.env.NO_COLOR !== '';
  colorEnabled = !(argvHasNoColor || envHasNoColor);
}
```

**TokenCostResult shape (for Gap #5 aggregation):**
From `packages/internal/src/token/types.ts:21-24` and `src/scanner/types.ts:8-36`:
```typescript
export interface InventoryItem {
  name: string;
  path: string;
  scope: ItemScope;
  category: ItemCategory;   // 'agent' | 'skill' | 'mcp-server' | 'memory'
  projectPath: string | null;
  mtimeMs?: number;
}
export interface ScanResult {
  item: InventoryItem;
  tier: GhostTier;   // 'used' | 'likely-ghost' | 'definite-ghost'
  lastUsed: Date | null;
  invocationCount: number;
}
export interface TokenCostResult extends ScanResult {
  tokenEstimate: TokenEstimate | null;
}
export interface TokenEstimate {
  tokens: number;
  confidence: ConfidenceTier;
  source: string;
}
```

**Current mcp.ts output routing (before aggregation is inserted):**
From `apps/ccaudit/src/cli/commands/mcp.ts:108-253`:
```typescript
// Step 4: Filter to MCP server items only
const mcpResults = results.filter(r => r.item.category === 'mcp-server');

// Step 5: Enrich with token estimates
let enriched: TokenCostResult[] = await enrichScanResults(mcpResults);

// Step 6: Live measurement (if --live flag)   <-- aggregation goes AFTER this block
if (ctx.values.live) { /* ... */ }

// Determine exit code: 1 if ghosts found (per D-01)
const hasGhosts = enriched.some(r => r.tier !== 'used');
// ... 4-way output routing: json -> csv -> quiet TSV -> rendered
```

JSON output block at mcp.ts:184-197 (current shape of each item — must add `projectPaths`):
```typescript
items: enriched.map(r => ({
  name: r.item.name,
  scope: r.item.scope,
  tier: r.tier,
  invocations: r.invocationCount,
  lastUsed: r.lastUsed?.toISOString() ?? null,
  projectPath: r.item.projectPath,
  tokenEstimate: r.tokenEstimate ? { tokens: ..., confidence: ..., source: ... } : null,
  recommendation: classifyRecommendation(r.tier),
})),
```

**Scanner per-project dedup (MUST NOT CHANGE — Phase 8 RMED-06 needs it):**
From `packages/internal/src/scanner/scan-mcp.ts:45-109`:
```typescript
export async function scanMcpServers(
  claudeConfigPath: string | undefined,
  projectPaths: string[],
): Promise<InventoryItem[]> {
  // ...
  const seen = new Set<string>();
  // Uses composite key `${projectPath}::${serverName}` for Phase 8 config-key traceability.
  // Fix for Gap #5 is presentation-layer ONLY. Do NOT touch this file.
}
```

**Package.json build script targets:**
From `packages/internal/package.json`:
```json
{
  "scripts": {
    "test": "TZ=UTC vitest",
    "typecheck": "tsc"
  }
}
```
From `packages/terminal/package.json`:
```json
{
  "scripts": {
    "test": "TZ=UTC vitest",
    "typecheck": "tsc --noEmit"
  }
}
```

**Both tsconfig.json files have `composite: true` + `emitDeclarationOnly: true` + `declaration: true`** (verified by reading both files), so `"build": "tsc"` is safe and idempotent for both packages (produces `.d.ts` declarations per composite project conventions; no `.js` output is needed because apps/ccaudit's tsdown bundler pulls `.ts` sources via rolldown).

**Test baseline:** `pnpm exec vitest --run` currently reports `Test Files 43 passed (43) · Tests 357 passed (357)`. Tasks 2/3/4/5 add tests → final count must be >= 357 with zero regressions.

**Approved plan (canonical spec):** `/Users/helldrik/.claude/plans/stateless-watching-koala.md` — every concrete value (file paths, line numbers, code snippets, verification commands) in the tasks below is extracted verbatim from that document.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add 'build': 'tsc' stubs to packages/internal and packages/terminal for Gap #6</name>
  <files>
    packages/internal/package.json
    packages/terminal/package.json
  </files>
  <read_first>
    /Users/helldrik/.claude/plans/stateless-watching-koala.md
    packages/internal/package.json
    packages/terminal/package.json
    packages/internal/tsconfig.json
    packages/terminal/tsconfig.json
    package.json
  </read_first>
  <action>
    Gap #6 root cause: root `package.json:11` defines `"build": "pnpm -r build"` and CI `.github/workflows/ci.yaml:61` runs the same. `pnpm -r <script>` errors with `ERR_PNPM_RECURSIVE_RUN_NO_SCRIPT` when NO package has the script; it silently skips when SOME have it. Currently only `apps/ccaudit` has a build script, so the root command works but fails when invoked from a subpackage directory where `-r` scoping excludes `apps/ccaudit`. Fix is to add symmetric `build` stubs to the two internal packages.

    **Pre-execution verification (already done in planning; re-verify during execute):**
    - `packages/internal/tsconfig.json` has `composite: true`, `emitDeclarationOnly: true`, `declaration: true` → `tsc` is safe (emits `.d.ts` only).
    - `packages/terminal/tsconfig.json` has identical settings → same applies.
    - Neither package emits `.js` runtime code; `apps/ccaudit`'s tsdown bundler resolves workspace imports via `.ts` source paths directly.

    **Edit 1: `packages/internal/package.json`**

    Current `scripts` block:
    ```json
    "scripts": {
      "test": "TZ=UTC vitest",
      "typecheck": "tsc"
    }
    ```

    Change to:
    ```json
    "scripts": {
      "build": "tsc",
      "test": "TZ=UTC vitest",
      "typecheck": "tsc"
    }
    ```

    The `build` key is added as the FIRST script (alphabetical convention, and matches the order in `apps/ccaudit/package.json`).

    **Edit 2: `packages/terminal/package.json`**

    Current `scripts` block:
    ```json
    "scripts": {
      "test": "TZ=UTC vitest",
      "typecheck": "tsc --noEmit"
    }
    ```

    Change to:
    ```json
    "scripts": {
      "build": "tsc",
      "test": "TZ=UTC vitest",
      "typecheck": "tsc --noEmit"
    }
    ```

    Note: the `typecheck` for terminal uses `--noEmit` (for fast type-only check) but the new `build` uses bare `tsc` (which respects `emitDeclarationOnly: true` in tsconfig and emits `.d.ts` files). Both invocations are compatible with composite project references and produce deterministic output.

    Do NOT touch root `package.json` — the existing `"build": "pnpm -r build"` now fans out to 3 packages symmetrically.

    Do NOT touch `.github/workflows/ci.yaml` — it keeps working, only more robustly.

    **Validation after editing both files:**
    ```bash
    node -e "const p = JSON.parse(require('fs').readFileSync('packages/internal/package.json', 'utf-8')); console.log('internal scripts:', Object.keys(p.scripts || {}));"
    node -e "const p = JSON.parse(require('fs').readFileSync('packages/terminal/package.json', 'utf-8')); console.log('terminal scripts:', Object.keys(p.scripts || {}));"
    ```
    Expected output:
    ```
    internal scripts: [ 'build', 'test', 'typecheck' ]
    terminal scripts: [ 'build', 'test', 'typecheck' ]
    ```

    **Smoke test (the canonical Gap #6 verification):**
    ```bash
    pnpm -r build
    echo "ROOT EXIT=$?"

    cd packages/internal && pnpm -r build && cd -
    cd packages/terminal && pnpm -r build && cd -
    ```
    Expected: all three invocations exit 0 with no `ERR_PNPM_RECURSIVE_RUN_NO_SCRIPT` in output.

    This task MUST come first — subsequent tasks' typecheck and test runs rely on `pnpm -r build` being fixed.
  </action>
  <verify>
    <automated>node -e "const i = JSON.parse(require('fs').readFileSync('packages/internal/package.json','utf-8')); const t = JSON.parse(require('fs').readFileSync('packages/terminal/package.json','utf-8')); if (i.scripts.build !== 'tsc') process.exit(1); if (t.scripts.build !== 'tsc') process.exit(2);" && pnpm -r build 2>&1 | tee /tmp/ccaudit-gap6-build.log && ! grep -q "ERR_PNPM_RECURSIVE_RUN_NO_SCRIPT" /tmp/ccaudit-gap6-build.log</automated>
  </verify>
  <done>
    - packages/internal/package.json has `"build": "tsc"` in scripts (3 total scripts: build, test, typecheck)
    - packages/terminal/package.json has `"build": "tsc"` in scripts (3 total scripts: build, test, typecheck)
    - Both files still parse as valid JSON
    - `pnpm -r build` exits 0 from workspace root with no skip error
    - `pnpm -r build` exits 0 from packages/internal directory
    - `pnpm -r build` exits 0 from packages/terminal directory
    - No other files modified
  </done>
  <acceptance_criteria>
    - `grep -q '"build": "tsc"' packages/internal/package.json` exits 0
    - `grep -q '"build": "tsc"' packages/terminal/package.json` exits 0
    - `node -e "JSON.parse(require('fs').readFileSync('packages/internal/package.json','utf-8'))"` exits 0 (valid JSON)
    - `node -e "JSON.parse(require('fs').readFileSync('packages/terminal/package.json','utf-8'))"` exits 0 (valid JSON)
    - `pnpm -r build` exits 0 from workspace root
    - `pnpm -r build 2>&1 | grep -c ERR_PNPM_RECURSIVE_RUN_NO_SCRIPT` returns 0
    - `(cd packages/internal && pnpm -r build)` exits 0
    - `(cd packages/terminal && pnpm -r build)` exits 0
    - Root `package.json` unchanged (`grep -q '"build": "pnpm -r build"' package.json` exits 0)
    - `.github/workflows/ci.yaml` unchanged
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add 'no-color' to outputArgs and flip in-source test for Gap #4</name>
  <files>
    apps/ccaudit/src/cli/_shared-args.ts
  </files>
  <read_first>
    /Users/helldrik/.claude/plans/stateless-watching-koala.md
    apps/ccaudit/src/cli/_shared-args.ts
    packages/terminal/src/color.ts
    .planning/phases/06-output-control-polish/06-CONTEXT.md
  </read_first>
  <behavior>
    - After the edit, `outputArgs` HAS a `'no-color'` key with `type: 'boolean'`, `default: false`, and a non-empty `description` mentioning both the flag behavior and the `NO_COLOR` env var.
    - The existing `quiet`, `csv`, `ci` keys are unchanged.
    - The in-source test that previously asserted `expect(outputArgs).not.toHaveProperty('no-color')` is FLIPPED to `expect(outputArgs).toHaveProperty('no-color')` plus shape assertions (type boolean, default false, description non-empty).
    - The header comment block is updated to explain the new two-source design: "declared here for --help visibility; initColor() reads process.argv directly for authoritative runtime behavior".
    - `packages/terminal/src/color.ts` is NOT touched (D-07 runtime contract preserved — initColor() is still the runtime source of truth).
    - No command file (ghost.ts, inventory.ts, mcp.ts, trend.ts) is touched in this task — they already spread `...outputArgs` and will pick up the new key automatically.
  </behavior>
  <action>
    Gap #4 root cause: D-07 mandated `initColor()` reads `process.argv.includes('--no-color')` directly so the flag works at any CLI position (`ccaudit --no-color ghost` and `ccaudit ghost --no-color` both work). Side effect: gunshi never sees `--no-color` as a declared arg → absent from every `--help` output. The tester hit this immediately in section #3 of the manual test.

    The fix keeps `initColor()` as the authoritative runtime source (gunshi parsing is additive, not a replacement — `process.argv` is unchanged by gunshi parsing, so both sources agree). We only add help metadata.

    **Edit the entire file at `apps/ccaudit/src/cli/_shared-args.ts`:**

    Replace the header comment block (lines 1-8) with:
    ```typescript
    /**
     * Shared CLI flag definitions for all subcommands.
     *
     * These flags are spread into every command's `args` object.
     *
     * --no-color is declared here for --help visibility (gunshi help metadata).
     * The authoritative runtime source is initColor() in @ccaudit/terminal/color.ts,
     * which reads process.argv directly for root-level positioning robustness (per D-07).
     * Both sources agree because gunshi parsing does not modify process.argv.
     */
    ```

    Extend the `outputArgs` object to include a new `'no-color'` key. Final shape of `outputArgs`:
    ```typescript
    export const outputArgs = {
      quiet: {
        type: 'boolean' as const,
        short: 'q',
        description: 'Machine-readable output only (suppress decorative text)',
        default: false,
      },
      csv: {
        type: 'boolean' as const,
        description: 'Output as CSV (RFC 4180)',
        default: false,
      },
      ci: {
        type: 'boolean' as const,
        description: 'CI mode: --json --quiet with exit codes (implies --json --quiet)',
        default: false,
      },
      'no-color': {
        type: 'boolean' as const,
        description: 'Disable ANSI colors in output (also respects NO_COLOR env var)',
        default: false,
      },
    } as const;
    ```

    Note: the key is the literal string `'no-color'` (kebab-case in quotes), which is how gunshi declares multi-word flags that expose on the CLI as `--no-color`. Do NOT use `noColor` as the key (camelCase) — the existing pattern in other commands and the tester's expectation both require the `--no-color` CLI form.

    **Flip the in-source test at lines 64-67:**

    Replace the existing test:
    ```typescript
    it('does NOT have no-color key', () => {
      expect(outputArgs).not.toHaveProperty('no-color');
      expect(outputArgs).not.toHaveProperty('noColor');
    });
    ```

    With:
    ```typescript
    it('has no-color key with type boolean and default false', () => {
      expect(outputArgs).toHaveProperty('no-color');
      expect((outputArgs as Record<string, unknown>)['no-color']).toMatchObject({
        type: 'boolean',
        default: false,
      });
    });

    it('no-color description mentions NO_COLOR env var', () => {
      const desc = (outputArgs as Record<string, { description?: string }>)['no-color'].description;
      expect(typeof desc).toBe('string');
      expect(desc).toContain('NO_COLOR');
    });
    ```

    Do NOT touch any other test in this file — the existing tests for `quiet`, `csv`, `ci` keys must continue to pass unchanged.

    **Do NOT touch `packages/terminal/src/color.ts`** — the D-07 runtime contract (initColor reading process.argv directly) is preserved. The fix is additive: gunshi now declares the flag for help metadata, but runtime behavior still flows through initColor.

    **Do NOT touch any command file** — `ghost.ts`, `inventory.ts`, `mcp.ts`, `trend.ts` already spread `...outputArgs` (verified in ghost.ts line 45, inventory.ts line 28, mcp.ts line 32, trend.ts line 28). The new key propagates automatically.

    **After editing, run the in-source tests for this file:**
    ```bash
    pnpm -F ccaudit exec vitest run --no-coverage src/cli/_shared-args.ts
    ```
    Expected: all 7 tests pass (the 5 existing + 2 new, replacing the old "does NOT have no-color" test).

    Then run the full suite:
    ```bash
    pnpm exec vitest --run
    ```
    Expected: 357 baseline tests + 1 new (net +1 because 1 old test is replaced by 2 new tests) = 358 tests passing.
  </action>
  <verify>
    <automated>grep -q "'no-color'" apps/ccaudit/src/cli/_shared-args.ts && grep -q "toHaveProperty('no-color')" apps/ccaudit/src/cli/_shared-args.ts && ! grep -q "not.toHaveProperty('no-color')" apps/ccaudit/src/cli/_shared-args.ts && grep -q "NO_COLOR" apps/ccaudit/src/cli/_shared-args.ts && pnpm -F ccaudit exec vitest run --no-coverage src/cli/_shared-args.ts 2>&1 | grep -q "passed"</automated>
  </verify>
  <done>
    - `apps/ccaudit/src/cli/_shared-args.ts` contains a `'no-color'` key in `outputArgs` with type boolean and default false
    - The description for `'no-color'` mentions `NO_COLOR` env var
    - The flipped test asserts `expect(outputArgs).toHaveProperty('no-color')` (positive assertion)
    - The old negative assertion `expect(outputArgs).not.toHaveProperty('no-color')` is GONE
    - Header comment updated to explain the two-source design (initColor is authoritative runtime, gunshi provides help metadata)
    - packages/terminal/src/color.ts is unchanged
    - No command files touched
    - In-source vitest for _shared-args.ts passes (all assertions green)
  </done>
  <acceptance_criteria>
    - `grep -q "'no-color':" apps/ccaudit/src/cli/_shared-args.ts` exits 0
    - `grep -q "toHaveProperty('no-color')" apps/ccaudit/src/cli/_shared-args.ts` exits 0
    - `grep -c "not.toHaveProperty('no-color')" apps/ccaudit/src/cli/_shared-args.ts` returns 0 (the old test is gone)
    - `grep -q "NO_COLOR" apps/ccaudit/src/cli/_shared-args.ts` exits 0 (description mentions env var)
    - `grep -q "initColor()" apps/ccaudit/src/cli/_shared-args.ts` exits 0 (header comment references the runtime source)
    - `git diff packages/terminal/src/color.ts` is empty (file unchanged)
    - `git diff apps/ccaudit/src/cli/commands/ghost.ts` is empty
    - `git diff apps/ccaudit/src/cli/commands/inventory.ts` is empty
    - `git diff apps/ccaudit/src/cli/commands/mcp.ts` is empty
    - `git diff apps/ccaudit/src/cli/commands/trend.ts` is empty
    - `pnpm -F ccaudit exec vitest run --no-coverage src/cli/_shared-args.ts 2>&1 | grep -Ec "passed"` returns > 0
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add aggregateMcpByName helper + call site + projectPaths JSON field + in-source tests for Gap #5</name>
  <files>
    apps/ccaudit/src/cli/commands/mcp.ts
  </files>
  <read_first>
    /Users/helldrik/.claude/plans/stateless-watching-koala.md
    apps/ccaudit/src/cli/commands/mcp.ts
    packages/internal/src/scanner/scan-mcp.ts
    packages/internal/src/token/types.ts
    packages/internal/src/scanner/types.ts
  </read_first>
  <behavior>
    - New top-level helper `aggregateMcpByName(enriched: TokenCostResult[]): TokenCostResult[]` in `commands/mcp.ts`, defined ABOVE the `mcpCommand` definition (around line 27).
    - Grouping key: `r.item.name` (MCP server name is globally unique per the MCP spec).
    - Merge semantics for duplicate-name groups:
      - `tier`: least-ghost wins — priority order `used` > `likely-ghost` > `definite-ghost`. One `used` instance anywhere makes the aggregate `used`.
      - `lastUsed`: max across the group (most recent Date), or null if all are null.
      - `invocationCount`: sum across the group.
      - `tokenEstimate`: identical across instances (same server name → same bundled estimate), take from any representative.
      - `item.projectPath`: set to `null` in the aggregated row (global view).
      - A new field `item.projectPaths: string[]` is attached via intersection typing for JSON traceability (array of all source projectPath values from the group, non-null only).
      - Other `item` fields: take any representative (scope, path, category).
    - Single-instance servers pass through unchanged (groups of size 1 return the original entry with `projectPaths: [projectPath]` populated).
    - Call site: inserted AFTER the live-measurement `if (ctx.values.live) { ... }` block and BEFORE the `const hasGhosts = enriched.some(...)` line — so it runs after both enrichment paths (estimate and live) complete.
    - Only the JSON `items.map` block and the underlying aggregated `enriched` array are affected; CSV, TSV, and rendered-table branches iterate the same aggregated array automatically.
    - JSON output adds a new `projectPaths: r.item.projectPaths ?? [r.item.projectPath].filter(Boolean)` field per item for traceability.
    - New in-source tests (2): (1) merges 2 entries with same name / different projectPaths into 1 row with max lastUsed and lowest-severity tier, (2) passes through a single-instance server unchanged.
    - `packages/internal/src/scanner/scan-mcp.ts` is NOT touched (Phase 8 RMED-06 needs per-project entries).
    - `packages/terminal/src/tables/mcp-table.ts` is NOT touched (already receives aggregated input).
  </behavior>
  <action>
    Gap #5 root cause: `scan-mcp.ts` uses a `Set<string>` keyed on `${projectPath}::${serverName}` (lines 70, 91) → per-project dedup only. Same MCP server in two `.mcp.json` files produces 2 items. `enrichScanResults` is a 1:1 Promise.all map. `mcp.ts` iterates `enriched` with no aggregation → duplicate rows in CSV/table/JSON/TSV. Phase 8 needs the per-project entries for `RMED-06` (config-key traceability), so the fix is strictly presentation-layer.

    **Edit `apps/ccaudit/src/cli/commands/mcp.ts`:**

    **Step 1: Insert the aggregation helper ABOVE `mcpCommand`.**

    Add the following helper function between the import block and the `export const mcpCommand = define({...})` declaration (around current line 27, just before `export const mcpCommand`):

    ```typescript
    /**
     * Aggregate enriched MCP results by server name for presentation output.
     *
     * Rationale (Gap #5 fix): scan-mcp.ts dedups per (projectPath::serverName) for Phase 8
     * RMED-06 traceability, producing 1 row per (server, project) pair. For the user-facing
     * mcp view, the same server in 2 .mcp.json files is 1 logical server with 2 source
     * configs — collapse by name here so CSV/table/JSON/TSV output shows one row per server.
     *
     * Merge semantics:
     *   tier            — least ghost wins: used > likely-ghost > definite-ghost
     *   lastUsed        — max (most recent) across the group
     *   invocationCount — sum across the group
     *   tokenEstimate   — identical per server name, take any representative
     *   projectPath     — null (aggregated view)
     *   projectPaths    — array of source projectPath values, for JSON traceability
     *   other item fields — any representative
     *
     * Scanner stays untouched — Phase 8 RMED-06 still knows which config key(s) to rewrite.
     */
    export function aggregateMcpByName(enriched: TokenCostResult[]): TokenCostResult[] {
      const tierRank = { 'used': 0, 'likely-ghost': 1, 'definite-ghost': 2 } as const;
      const groups = new Map<string, TokenCostResult[]>();
      for (const r of enriched) {
        const key = r.item.name;
        const bucket = groups.get(key);
        if (bucket) {
          bucket.push(r);
        } else {
          groups.set(key, [r]);
        }
      }

      const aggregated: TokenCostResult[] = [];
      for (const group of groups.values()) {
        if (group.length === 1) {
          // Single-instance server: pass through, but attach projectPaths for JSON traceability.
          const only = group[0]!;
          const projectPaths = only.item.projectPath ? [only.item.projectPath] : [];
          aggregated.push({
            ...only,
            item: { ...only.item, projectPaths } as typeof only.item & { projectPaths: string[] },
          });
          continue;
        }

        // Multi-instance: merge.
        let bestTier = group[0]!.tier;
        for (const r of group) {
          if (tierRank[r.tier] < tierRank[bestTier]) bestTier = r.tier;
        }

        let maxLastUsed: Date | null = null;
        for (const r of group) {
          if (r.lastUsed && (!maxLastUsed || r.lastUsed > maxLastUsed)) {
            maxLastUsed = r.lastUsed;
          }
        }

        const totalInvocations = group.reduce((sum, r) => sum + r.invocationCount, 0);
        const representative = group[0]!;
        const projectPaths = group.map(r => r.item.projectPath).filter((p): p is string => p !== null);

        aggregated.push({
          item: {
            ...representative.item,
            projectPath: null,
            projectPaths,
          } as typeof representative.item & { projectPaths: string[] },
          tier: bestTier,
          lastUsed: maxLastUsed,
          invocationCount: totalInvocations,
          tokenEstimate: representative.tokenEstimate,
        });
      }

      return aggregated;
    }
    ```

    **Step 2: Insert the call site between the `if (ctx.values.live) {...}` block and the `hasGhosts` calculation.**

    Current code around line 161-164:
    ```typescript
          }),
        );
      }

      // Determine exit code: 1 if ghosts found (per D-01)
      const hasGhosts = enriched.some(r => r.tier !== 'used');
    ```

    Change to:
    ```typescript
          }),
        );
      }

      // Gap #5 fix: aggregate per server name for presentation — scanner keeps per-project
      // dedup for Phase 8 RMED-06; this is the user-facing collapse so CSV/table/JSON/TSV
      // show one row per server, not one per (server, project) pair.
      enriched = aggregateMcpByName(enriched);

      // Determine exit code: 1 if ghosts found (per D-01)
      const hasGhosts = enriched.some(r => r.tier !== 'used');
    ```

    **Step 3: Extend the JSON `items.map` at lines 184-197 to include `projectPaths`.**

    Current shape:
    ```typescript
    items: enriched.map(r => ({
      name: r.item.name,
      scope: r.item.scope,
      tier: r.tier,
      invocations: r.invocationCount,
      lastUsed: r.lastUsed?.toISOString() ?? null,
      projectPath: r.item.projectPath,
      tokenEstimate: r.tokenEstimate ? {
        tokens: r.tokenEstimate.tokens,
        confidence: r.tokenEstimate.confidence,
        source: r.tokenEstimate.source,
      } : null,
      recommendation: classifyRecommendation(r.tier),
    })),
    ```

    Change to:
    ```typescript
    items: enriched.map(r => {
      const itemWithPaths = r.item as typeof r.item & { projectPaths?: string[] };
      return {
        name: r.item.name,
        scope: r.item.scope,
        tier: r.tier,
        invocations: r.invocationCount,
        lastUsed: r.lastUsed?.toISOString() ?? null,
        projectPath: r.item.projectPath,
        projectPaths: itemWithPaths.projectPaths ?? (r.item.projectPath ? [r.item.projectPath] : []),
        tokenEstimate: r.tokenEstimate ? {
          tokens: r.tokenEstimate.tokens,
          confidence: r.tokenEstimate.confidence,
          source: r.tokenEstimate.source,
        } : null,
        recommendation: classifyRecommendation(r.tier),
      };
    }),
    ```

    The new `projectPaths` field is an array. For aggregated multi-instance rows, it holds all source project paths. For single-instance rows, it holds the sole `projectPath` wrapped in an array (or `[]` if global). Document this in `docs/JSON-SCHEMA.md` in Task 6.

    **Step 4: Extend the in-source test block at lines 255-282 with 2 new tests for `aggregateMcpByName`.**

    Append to the existing `describe('mcp command wiring', () => { ... })` block (or create a new adjacent describe — either is acceptable). The new tests:

    ```typescript
    describe('aggregateMcpByName', () => {
      const makeResult = (
        name: string,
        projectPath: string | null,
        tier: 'used' | 'likely-ghost' | 'definite-ghost',
        invocationCount: number,
        lastUsed: Date | null,
      ): TokenCostResult => ({
        item: {
          name,
          path: '/fake/.mcp.json',
          scope: projectPath ? 'project' : 'global',
          category: 'mcp-server',
          projectPath,
        },
        tier,
        lastUsed,
        invocationCount,
        tokenEstimate: { tokens: 1000, confidence: 'estimated', source: 'bundled' },
      });

      it('merges duplicate server names, keeping max lastUsed and least-ghost tier', () => {
        const older = new Date('2026-01-01T00:00:00Z');
        const newer = new Date('2026-03-15T00:00:00Z');
        const input: TokenCostResult[] = [
          makeResult('context7', '/proj/a', 'definite-ghost', 0, null),
          makeResult('context7', '/proj/b', 'used', 5, newer),
          makeResult('context7', '/proj/c', 'likely-ghost', 1, older),
        ];

        const result = aggregateMcpByName(input);

        expect(result).toHaveLength(1);
        const row = result[0]!;
        expect(row.item.name).toBe('context7');
        expect(row.tier).toBe('used');
        expect(row.lastUsed).toEqual(newer);
        expect(row.invocationCount).toBe(6);
        expect(row.item.projectPath).toBeNull();
        const projectPaths = (row.item as typeof row.item & { projectPaths: string[] }).projectPaths;
        expect(projectPaths).toEqual(['/proj/a', '/proj/b', '/proj/c']);
      });

      it('passes through single-instance servers unchanged with projectPaths populated', () => {
        const when = new Date('2026-02-01T00:00:00Z');
        const input: TokenCostResult[] = [
          makeResult('playwright', '/proj/only', 'likely-ghost', 2, when),
        ];

        const result = aggregateMcpByName(input);

        expect(result).toHaveLength(1);
        const row = result[0]!;
        expect(row.item.name).toBe('playwright');
        expect(row.tier).toBe('likely-ghost');
        expect(row.lastUsed).toEqual(when);
        expect(row.invocationCount).toBe(2);
        const projectPaths = (row.item as typeof row.item & { projectPaths: string[] }).projectPaths;
        expect(projectPaths).toEqual(['/proj/only']);
      });
    });
    ```

    **Step 5: Validate with focused test run:**
    ```bash
    pnpm -F ccaudit exec vitest run --no-coverage src/cli/commands/mcp.ts
    ```
    Expected: the existing 3 mcp command wiring tests PLUS the 2 new aggregateMcpByName tests all pass.

    **Do NOT touch `packages/internal/src/scanner/scan-mcp.ts`** — scanner per-project dedup is preserved for Phase 8 RMED-06.

    **Do NOT touch `packages/terminal/src/tables/mcp-table.ts`** — it already receives aggregated input via the command layer.
  </action>
  <verify>
    <automated>grep -q "aggregateMcpByName" apps/ccaudit/src/cli/commands/mcp.ts && grep -q "enriched = aggregateMcpByName(enriched)" apps/ccaudit/src/cli/commands/mcp.ts && grep -q "projectPaths" apps/ccaudit/src/cli/commands/mcp.ts && grep -q "tierRank" apps/ccaudit/src/cli/commands/mcp.ts && pnpm -F ccaudit exec vitest run --no-coverage src/cli/commands/mcp.ts 2>&1 | grep -q "passed" && git diff packages/internal/src/scanner/scan-mcp.ts | wc -l | tr -d ' ' | grep -q '^0$'</automated>
  </verify>
  <done>
    - `aggregateMcpByName` function defined in `apps/ccaudit/src/cli/commands/mcp.ts` above `mcpCommand`
    - Call site `enriched = aggregateMcpByName(enriched)` inserted after `if (ctx.values.live) {...}` block and before `hasGhosts` computation
    - JSON `items.map` now includes `projectPaths` field in each item
    - Two new in-source tests for `aggregateMcpByName` (merge case + single-instance passthrough)
    - Scanner (`scan-mcp.ts`) unchanged — `git diff` is empty
    - Terminal MCP table renderer (`mcp-table.ts`) unchanged
    - Focused vitest for mcp.ts passes
  </done>
  <acceptance_criteria>
    - `grep -q "export function aggregateMcpByName" apps/ccaudit/src/cli/commands/mcp.ts` exits 0
    - `grep -q "enriched = aggregateMcpByName(enriched)" apps/ccaudit/src/cli/commands/mcp.ts` exits 0
    - `grep -q "projectPaths: itemWithPaths" apps/ccaudit/src/cli/commands/mcp.ts` exits 0 OR `grep -q "projectPaths: projectPaths" apps/ccaudit/src/cli/commands/mcp.ts` exits 0
    - `grep -q "merges duplicate server names" apps/ccaudit/src/cli/commands/mcp.ts` exits 0
    - `grep -q "passes through single-instance" apps/ccaudit/src/cli/commands/mcp.ts` exits 0
    - `git diff packages/internal/src/scanner/scan-mcp.ts | wc -l` returns 0 (file unchanged)
    - `git diff packages/terminal/src/tables/mcp-table.ts | wc -l` returns 0 (file unchanged)
    - `pnpm -F ccaudit exec vitest run --no-coverage src/cli/commands/mcp.ts 2>&1 | grep -Ec "passed"` returns > 0
    - `pnpm -F ccaudit exec vitest run --no-coverage src/cli/commands/mcp.ts 2>&1 | grep -q "aggregateMcpByName"` exits 0 (test names surface)
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Create E2E regression test for mcp --csv cross-project dedup (Gap #5)</name>
  <files>
    apps/ccaudit/src/__tests__/mcp-aggregation.test.ts
  </files>
  <read_first>
    /Users/helldrik/.claude/plans/stateless-watching-koala.md
    apps/ccaudit/src/__tests__/ghost-command.test.ts
    apps/ccaudit/src/__tests__/dry-run-command.test.ts
    apps/ccaudit/src/cli/commands/mcp.ts
  </read_first>
  <behavior>
    - New file `apps/ccaudit/src/__tests__/mcp-aggregation.test.ts`.
    - Test strategy: use the aggregateMcpByName helper directly (imported from the command file) rather than spawning dist/index.js, because the aggregation fix is at the helper level and the E2E filesystem fixture for cross-project .mcp.json files would be larger than the fix itself.
    - Test 1: Build a TokenCostResult[] containing the same server `supabase` from two different projectPaths (`/home/user/projA` and `/home/user/projB`), both tier=definite-ghost, invocationCount=0, lastUsed=null. Call `aggregateMcpByName`. Assert: result.length === 1, row.item.name === 'supabase', row.item.projectPath === null, row.item.projectPaths.length === 2, row.tier === 'definite-ghost'.
    - Test 2: Same server `context7` in three projects: one tier=used (invocations=10, lastUsed=recent), two tier=definite-ghost (invocations=0, lastUsed=null). Assert: result.length === 1, row.tier === 'used' (least-ghost wins), row.invocationCount === 10 (sum), row.lastUsed equals the recent date.
    - Test 3: CSV output regression check — given a mixed set (supabase×2 duplicate + context7×1 unique + playwright×2 duplicate), the aggregation produces exactly 3 unique rows and a simulated CSV column-1 extraction shows no duplicates (via `new Set(rows.map(r => r.item.name)).size === aggregated.length`).
    - The file follows the vitest pattern used in `dry-run-command.test.ts` (describe / it blocks, `import { describe, it, expect } from 'vitest'`).
    - All 3 tests pass.
  </behavior>
  <action>
    Create `apps/ccaudit/src/__tests__/mcp-aggregation.test.ts` with the following content verbatim:

    ```typescript
    /**
     * E2E regression test for Gap #5: ccaudit mcp --csv cross-project duplicate rows.
     *
     * Scenario: when the same MCP server is declared in multiple .mcp.json files
     * (or in multiple ~/.claude.json projects entries), the scanner emits one
     * InventoryItem per (projectPath, serverName) pair for Phase 8 RMED-06
     * traceability. The presentation layer must collapse by name so CSV/table/JSON
     * show one row per server, not one per (server, project) pair.
     *
     * This test exercises aggregateMcpByName directly — the E2E filesystem fixture
     * for multi-.mcp.json cross-project setup would be significantly larger than
     * the fix itself, and the helper-level test gives the same coverage.
     */
    import { describe, it, expect } from 'vitest';
    import type { TokenCostResult } from '@ccaudit/internal';
    import { aggregateMcpByName } from '../cli/commands/mcp.ts';

    function makeResult(
      name: string,
      projectPath: string | null,
      tier: 'used' | 'likely-ghost' | 'definite-ghost',
      invocationCount: number,
      lastUsed: Date | null,
    ): TokenCostResult {
      return {
        item: {
          name,
          path: projectPath ? `${projectPath}/.mcp.json` : '/home/user/.claude.json',
          scope: projectPath ? 'project' : 'global',
          category: 'mcp-server',
          projectPath,
        },
        tier,
        lastUsed,
        invocationCount,
        tokenEstimate: { tokens: 1500, confidence: 'estimated', source: 'bundled' },
      };
    }

    describe('Gap #5 regression: mcp --csv cross-project duplicate rows', () => {
      it('collapses duplicate server name across two .mcp.json files into one row', () => {
        const input: TokenCostResult[] = [
          makeResult('supabase', '/home/user/projA', 'definite-ghost', 0, null),
          makeResult('supabase', '/home/user/projB', 'definite-ghost', 0, null),
        ];

        const result = aggregateMcpByName(input);

        expect(result).toHaveLength(1);
        const row = result[0]!;
        expect(row.item.name).toBe('supabase');
        expect(row.item.projectPath).toBeNull();
        const projectPaths = (row.item as typeof row.item & { projectPaths: string[] }).projectPaths;
        expect(projectPaths).toHaveLength(2);
        expect(projectPaths).toContain('/home/user/projA');
        expect(projectPaths).toContain('/home/user/projB');
        expect(row.tier).toBe('definite-ghost');
      });

      it('keeps least-ghost tier when one instance is used and others are ghosts', () => {
        const recent = new Date('2026-03-20T10:00:00Z');
        const input: TokenCostResult[] = [
          makeResult('context7', '/home/user/projA', 'definite-ghost', 0, null),
          makeResult('context7', '/home/user/projB', 'used', 10, recent),
          makeResult('context7', '/home/user/projC', 'definite-ghost', 0, null),
        ];

        const result = aggregateMcpByName(input);

        expect(result).toHaveLength(1);
        const row = result[0]!;
        expect(row.tier).toBe('used');
        expect(row.invocationCount).toBe(10);
        expect(row.lastUsed).toEqual(recent);
      });

      it('a mixed set of duplicates and uniques produces the correct collapsed row count', () => {
        const input: TokenCostResult[] = [
          makeResult('supabase', '/p/a', 'definite-ghost', 0, null),
          makeResult('supabase', '/p/b', 'definite-ghost', 0, null),
          makeResult('context7', '/p/a', 'likely-ghost', 1, new Date('2026-02-15T00:00:00Z')),
          makeResult('playwright', '/p/c', 'definite-ghost', 0, null),
          makeResult('playwright', '/p/d', 'definite-ghost', 0, null),
        ];

        const result = aggregateMcpByName(input);

        expect(result).toHaveLength(3);
        const names = result.map(r => r.item.name).sort();
        expect(names).toEqual(['context7', 'playwright', 'supabase']);

        // Regression check: no duplicate names after aggregation.
        const uniqueNames = new Set(result.map(r => r.item.name));
        expect(uniqueNames.size).toBe(result.length);
      });
    });
    ```

    Verify the file parses and runs:
    ```bash
    pnpm -F ccaudit exec vitest run --no-coverage src/__tests__/mcp-aggregation.test.ts
    ```
    Expected: 3 tests, all passing.

    Note: this import `import { aggregateMcpByName } from '../cli/commands/mcp.ts'` requires `aggregateMcpByName` to be exported from `mcp.ts` — which Task 3 does via `export function aggregateMcpByName(...)`. If Task 3 used an un-exported helper, that's a planning error — Task 3's action block specifies `export function aggregateMcpByName` explicitly.
  </action>
  <verify>
    <automated>test -f apps/ccaudit/src/__tests__/mcp-aggregation.test.ts && grep -q "aggregateMcpByName" apps/ccaudit/src/__tests__/mcp-aggregation.test.ts && grep -q "supabase" apps/ccaudit/src/__tests__/mcp-aggregation.test.ts && pnpm -F ccaudit exec vitest run --no-coverage src/__tests__/mcp-aggregation.test.ts 2>&1 | grep -q "3 passed"</automated>
  </verify>
  <done>
    - File `apps/ccaudit/src/__tests__/mcp-aggregation.test.ts` exists
    - Contains 3 test cases: duplicate collapse, least-ghost tier selection, mixed set count
    - Imports `aggregateMcpByName` from `../cli/commands/mcp.ts`
    - All 3 tests pass
    - Uses the same describe/it pattern as `dry-run-command.test.ts`
  </done>
  <acceptance_criteria>
    - `test -f apps/ccaudit/src/__tests__/mcp-aggregation.test.ts` exits 0
    - `grep -q "import { aggregateMcpByName }" apps/ccaudit/src/__tests__/mcp-aggregation.test.ts` exits 0
    - `grep -c "it(" apps/ccaudit/src/__tests__/mcp-aggregation.test.ts` returns >= 3
    - `grep -q "collapses duplicate server name" apps/ccaudit/src/__tests__/mcp-aggregation.test.ts` exits 0
    - `grep -q "least-ghost tier" apps/ccaudit/src/__tests__/mcp-aggregation.test.ts` exits 0
    - `pnpm -F ccaudit exec vitest run --no-coverage src/__tests__/mcp-aggregation.test.ts 2>&1 | grep -q "3 passed"` exits 0
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 5: Create E2E help-output regression test for --no-color visibility (Gap #4)</name>
  <files>
    apps/ccaudit/src/__tests__/help-output.test.ts
  </files>
  <read_first>
    /Users/helldrik/.claude/plans/stateless-watching-koala.md
    apps/ccaudit/src/__tests__/dry-run-command.test.ts
    apps/ccaudit/src/cli/_shared-args.ts
  </read_first>
  <behavior>
    - New file `apps/ccaudit/src/__tests__/help-output.test.ts`.
    - Test strategy: spawn `node apps/ccaudit/dist/index.js <args>` as a child process, capture stdout, grep for flag names.
    - Pre-test step: call `pnpm -F ccaudit build` (via execSync) or use an already-built dist — follows dry-run-command.test.ts precedent.
    - Test 1: root `--help` output contains `no-color`.
    - Test 2: `ghost --help` contains `no-color`.
    - Test 3: `inventory --help` contains `no-color`.
    - Test 4: `mcp --help` contains `no-color`.
    - Test 5: `trend --help` contains `no-color`.
    - All 5 tests pass.
    - This test serves as future regression guard: if any subcommand ever loses the outputArgs spread, the test flags it.
  </behavior>
  <action>
    Create `apps/ccaudit/src/__tests__/help-output.test.ts` with the following content verbatim:

    ```typescript
    /**
     * E2E regression test for Gap #4: --no-color visibility in --help output.
     *
     * D-07 mandates initColor() reads process.argv directly for root-level positioning,
     * but the original implementation at _shared-args.ts EXCLUDED 'no-color' from gunshi's
     * declared args, making the flag invisible in every --help output. Gap #4 fix adds
     * 'no-color' to outputArgs (declaration only — initColor remains the runtime source).
     *
     * This test spawns the built dist/index.js binary and greps --help output on root
     * plus every subcommand for the 'no-color' substring. Serves as permanent regression
     * guard: if any subcommand loses the outputArgs spread, this test fails.
     */
    import { describe, it, expect, beforeAll } from 'vitest';
    import { execSync, spawnSync } from 'node:child_process';
    import { join } from 'node:path';

    // Resolve the dist binary relative to the test file's effective cwd.
    // vitest runs tests from the workspace root by convention.
    const BINARY = join(process.cwd(), 'apps/ccaudit/dist/index.js');

    function runHelp(args: string[]): string {
      const result = spawnSync('node', [BINARY, ...args], {
        encoding: 'utf-8',
        timeout: 10000,
        env: { ...process.env, NO_COLOR: '1' },
      });
      // Combine stdout + stderr; gunshi may emit help to either depending on version.
      return (result.stdout ?? '') + (result.stderr ?? '');
    }

    describe('Gap #4 regression: --no-color visible in --help', () => {
      beforeAll(() => {
        // Ensure the binary is built. This is idempotent — tsdown reuses cached output
        // when sources are unchanged. Other integration tests (dry-run-command.test.ts)
        // rely on the same invariant.
        execSync('pnpm -F ccaudit build', { stdio: 'pipe' });
      });

      it('root ccaudit --help lists no-color', () => {
        const output = runHelp(['--help']);
        expect(output).toContain('no-color');
      });

      it('ccaudit ghost --help lists no-color', () => {
        const output = runHelp(['ghost', '--help']);
        expect(output).toContain('no-color');
      });

      it('ccaudit inventory --help lists no-color', () => {
        const output = runHelp(['inventory', '--help']);
        expect(output).toContain('no-color');
      });

      it('ccaudit mcp --help lists no-color', () => {
        const output = runHelp(['mcp', '--help']);
        expect(output).toContain('no-color');
      });

      it('ccaudit trend --help lists no-color', () => {
        const output = runHelp(['trend', '--help']);
        expect(output).toContain('no-color');
      });
    });
    ```

    Verify:
    ```bash
    pnpm -F ccaudit build   # ensure dist/index.js reflects Task 2's --no-color addition
    pnpm -F ccaudit exec vitest run --no-coverage src/__tests__/help-output.test.ts
    ```
    Expected: 5 tests pass.

    **Important ordering note:** this test depends on Task 2 (outputArgs declaration) being complete AND on a fresh dist/ build that reflects Task 2's source changes. If the test is run before rebuilding, the old dist/ won't show `--no-color` in help and the test will fail. The `beforeAll` hook rebuilds to guarantee correctness.
  </action>
  <verify>
    <automated>test -f apps/ccaudit/src/__tests__/help-output.test.ts && grep -q "no-color" apps/ccaudit/src/__tests__/help-output.test.ts && grep -c "it(" apps/ccaudit/src/__tests__/help-output.test.ts | awk '$1 >= 5' && pnpm -F ccaudit build && pnpm -F ccaudit exec vitest run --no-coverage src/__tests__/help-output.test.ts 2>&1 | grep -q "5 passed"</automated>
  </verify>
  <done>
    - File `apps/ccaudit/src/__tests__/help-output.test.ts` exists
    - Contains 5 it() tests: root help + 4 subcommand helps
    - Uses spawnSync to invoke `node apps/ccaudit/dist/index.js --help`
    - `beforeAll` hook rebuilds dist/ via `pnpm -F ccaudit build`
    - All 5 tests pass after Task 2 + dist rebuild
  </done>
  <acceptance_criteria>
    - `test -f apps/ccaudit/src/__tests__/help-output.test.ts` exits 0
    - `grep -c "it(" apps/ccaudit/src/__tests__/help-output.test.ts` returns >= 5
    - `grep -q "root ccaudit --help lists no-color" apps/ccaudit/src/__tests__/help-output.test.ts` exits 0
    - `grep -q "trend --help lists no-color" apps/ccaudit/src/__tests__/help-output.test.ts` exits 0
    - `grep -q "spawnSync" apps/ccaudit/src/__tests__/help-output.test.ts` exits 0
    - `pnpm -F ccaudit exec vitest run --no-coverage src/__tests__/help-output.test.ts 2>&1 | grep -q "5 passed"` exits 0
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 6: Create docs/JSON-SCHEMA.md, add README section, correct docs/manual-test-results.md (Gap #3 Category A)</name>
  <files>
    docs/JSON-SCHEMA.md
    README.md
    docs/manual-test-results.md
  </files>
  <read_first>
    /Users/helldrik/.claude/plans/stateless-watching-koala.md
    apps/ccaudit/src/cli/_output-mode.ts
    apps/ccaudit/src/cli/commands/ghost.ts
    apps/ccaudit/src/cli/commands/inventory.ts
    apps/ccaudit/src/cli/commands/mcp.ts
    apps/ccaudit/src/cli/commands/trend.ts
    README.md
    docs/manual-test-results.md
    .planning/phases/06-output-control-polish/06-CONTEXT.md
  </read_first>
  <action>
    Gap #3 is NOT a bug — the code is correct per Phase 6 D-16 (camelCase `items` / `meta.timestamp` / `meta.exitCode`). The tester expected REST-API snake_case (`.data` / `.meta.generated_at` / `.meta.exit_code`) with no public documentation to correct that assumption. Fix is documentation-only: create the schema doc, link from README, correct the manual test results.

    **Step 1: Create `docs/JSON-SCHEMA.md` (new file, target <=80 lines).**

    Write this file verbatim:

    ```markdown
    # ccaudit JSON Output Schema

    All `ccaudit` read commands support structured JSON output via the `--json` flag
    (and the `--ci` convenience alias, which implies `--json --quiet`). This document
    describes the canonical envelope shape and is the source of truth for scripts
    and CI integrations that consume ccaudit output.

    > **Naming convention:** field names are **camelCase** to match TypeScript internals
    > and the `gh` CLI precedent. This is frozen as Phase 6 D-16 and will not change
    > in v1.x. A future v2 may add a `--json-snake-case` compatibility flag if demand
    > materializes.

    ## Envelope

    Every command wraps its payload in a standard `meta` envelope:

    ```json
    {
      "meta": {
        "command": "ghost",
        "version": "0.0.1",
        "since": "7d",
        "timestamp": "2026-04-05T10:30:00.000Z",
        "exitCode": 1
      },
      "items": [ ... ]
    }
    ```

    | Field            | Type     | Description                                                         |
    |------------------|----------|---------------------------------------------------------------------|
    | `meta.command`   | string   | Subcommand name: `ghost`, `inventory`, `mcp`, or `trend`            |
    | `meta.version`   | string   | ccaudit version (semver)                                            |
    | `meta.since`     | string   | Time window as passed to `--since` (e.g., `7d`, `30d`, `2w`)        |
    | `meta.timestamp` | string   | ISO 8601 UTC timestamp of the run                                   |
    | `meta.exitCode`  | number   | Process exit code: `0` = no ghosts, `1` = ghosts found              |

    ## Payload key by command

    | Command     | Payload key | Row shape                                                   |
    |-------------|-------------|-------------------------------------------------------------|
    | `ghost`     | `items`     | `{ name, category, scope, tier, invocations, lastUsed, tokenEstimate, recommendation }` |
    | `inventory` | `items`     | Same as `ghost` (full inventory, not just ghosts)           |
    | `mcp`       | `items`     | Adds `projectPaths: string[]` for cross-project traceability (see Note below) |
    | `trend`     | `buckets`   | `{ date, bucket, agents, skills, mcp, total }` per D-20     |

    **Note on `mcp.items[].projectPaths`:** the MCP scanner emits one entry per
    `(projectPath, serverName)` pair for Phase 8 remediation traceability. The `mcp`
    command aggregates by server name for presentation, exposing the original per-project
    list as `projectPaths: string[]`. A server declared globally returns `projectPaths: []`.

    ## jq examples

    ```sh
    # Count ghosts
    ccaudit ghost --json | jq '.items | length'

    # Get the exit code from JSON (alternative to shell $?)
    ccaudit ghost --json | jq '.meta.exitCode'

    # ISO timestamp of the run
    ccaudit ghost --json | jq '.meta.timestamp'

    # All definite-ghost MCP servers
    ccaudit mcp --json | jq '.items[] | select(.tier == "definite-ghost") | .name'

    # Trend data as CSV via jq
    ccaudit trend --json | jq -r '.buckets[] | [.date, .total] | @csv'
    ```

    ## Full example: `ccaudit ghost --json`

    ```json
    {
      "meta": {
        "command": "ghost",
        "version": "0.0.1",
        "since": "7d",
        "timestamp": "2026-04-05T10:30:00.000Z",
        "exitCode": 1
      },
      "window": "7d",
      "files": 463,
      "projects": 30,
      "inventory": 208,
      "ghosts": { "definite": 180, "likely": 16, "total": 196 },
      "healthScore": { "score": 11, "grade": "Critical", "ghostPenalty": 80, "tokenPenalty": 9 },
      "totalOverhead": { "tokens": 180000, "percentOf200k": 90 },
      "items": [
        {
          "name": "example-agent",
          "category": "agent",
          "tier": "definite-ghost",
          "invocations": 0,
          "lastUsed": null,
          "tokenEstimate": { "tokens": 1200, "confidence": "estimated", "source": "file-size" },
          "recommendation": "archive"
        }
      ]
    }
    ```
    ```

    **Word count check:** the file should be approximately 70-80 lines of markdown content (code blocks included). If it exceeds 90 lines, trim descriptions — but keep all tables, the envelope example, the jq examples, and the full example.

    **Step 2: Extend `README.md` with a "Machine-readable output" section.**

    Read the current `README.md` to find the right location. Look for an existing section after CLI usage / flag reference and before troubleshooting (check the structure — a section titled "Flags Reference" is a typical anchor). Add a new `## Machine-readable output` section after that one. Content:

    ```markdown
    ## Machine-readable output

    All read commands support structured JSON and spreadsheet-friendly CSV output:

    ```sh
    # JSON envelope with meta + items (see docs/JSON-SCHEMA.md for the full schema)
    ccaudit ghost --json | jq '.items | length'
    ccaudit ghost --json | jq '.meta.exitCode'

    # CSV for Google Sheets / Excel
    ccaudit ghost --csv > ghosts.csv

    # --ci is sugar for --json --quiet with exit codes (GitHub Actions, pre-commit)
    ccaudit --ci
    ```

    The JSON envelope uses **camelCase** field names (`items`, `meta.timestamp`,
    `meta.exitCode`) to match TypeScript internals and the `gh` CLI convention.
    See [JSON Schema](./docs/JSON-SCHEMA.md) for the canonical schema, per-command
    payload keys, and jq recipes.
    ```

    **Step 3: Correct `docs/manual-test-results.md` — reframe the three false-positive error entries.**

    The current file (lines 49-106 cover errors #2, #3, #4 per the approved plan) documents the tester's snake_case expectations as "FAIL". These were genuine misunderstandings, not code bugs. Update the file in two ways:

    **Change 3a:** In the "Summary" table near the top (line 19 in current file, the row "5. --json structured output | **FAIL** — field name mismatches"), change to:
    ```
    | 5. --json structured output | PASS (spec clarified in [JSON-SCHEMA.md](../docs/JSON-SCHEMA.md)) |
    ```

    **Change 3b:** For the "Errors and Failures" entries #2, #3, #4 (the ones about `.data`, `meta.generated_at`, `meta.exit_code`), reformat as spec-clarification notes rather than bugs. Replace the three entries with a single consolidated entry:

    ```markdown
    ### 2-4. JSON field names (spec clarification — NOT a bug)

    **Commands initially tested with incorrect expected values:**
    ```sh
    cc ghost --json | jq '.data'             # tester expected .data
    cc ghost --json | jq '.meta.generated_at'  # tester expected .meta.generated_at
    cc ghost --json | jq '.meta.exit_code'     # tester expected .meta.exit_code
    ```

    **Corrected after spec review:** the code is spec-compliant. Phase 6 decision
    D-16 mandates a **camelCase** envelope shape with `items` (not `data`),
    `meta.timestamp` (not `generated_at`), and `meta.exitCode` (not `exit_code`).
    This matches TypeScript internals and the `gh` CLI convention.

    **Correct commands:**
    ```sh
    cc ghost --json | jq '.items'          # payload array
    cc ghost --json | jq '.items | length' # row count
    cc ghost --json | jq '.meta.timestamp' # ISO 8601 run time
    cc ghost --json | jq '.meta.exitCode'  # 0 or 1
    ```

    See [`docs/JSON-SCHEMA.md`](./JSON-SCHEMA.md) for the full envelope reference
    and per-command payload keys. The test-spec expectations in earlier drafts of
    this file were based on REST-API snake_case conventions; ccaudit's JSON is a
    JS-native CLI output, not a cross-language API contract.
    ```

    Leave the genuine bug reports in the file (the Gap #6 `pnpm -r build` entry at section #5, and any others not covered by Gap #3/#4/#5) unchanged — they're tracked separately.

    **Step 4: Validate:**
    ```bash
    wc -l docs/JSON-SCHEMA.md             # should be <= 90 lines (~70-80 expected)
    grep -q "JSON-SCHEMA.md" README.md
    grep -q "JSON-SCHEMA.md" docs/manual-test-results.md
    grep -q "meta.exitCode" docs/JSON-SCHEMA.md
    grep -q "buckets" docs/JSON-SCHEMA.md
    grep -q "projectPaths" docs/JSON-SCHEMA.md
    ```

    Do NOT modify the JSON envelope code in `_output-mode.ts` — this is documentation-only. The locked test at `_output-mode.ts:120-145` must continue to pass unchanged.
  </action>
  <verify>
    <automated>test -f docs/JSON-SCHEMA.md && grep -q "meta.exitCode" docs/JSON-SCHEMA.md && grep -q "meta.timestamp" docs/JSON-SCHEMA.md && grep -q "camelCase" docs/JSON-SCHEMA.md && grep -q "buckets" docs/JSON-SCHEMA.md && grep -q "projectPaths" docs/JSON-SCHEMA.md && grep -q "JSON-SCHEMA.md" README.md && grep -q "JSON-SCHEMA" docs/manual-test-results.md && test $(wc -l < docs/JSON-SCHEMA.md) -le 120 && git diff apps/ccaudit/src/cli/_output-mode.ts | wc -l | tr -d ' ' | grep -q '^0$'</automated>
  </verify>
  <done>
    - `docs/JSON-SCHEMA.md` exists and is <= 120 lines
    - Contains camelCase field documentation (`items`, `meta.timestamp`, `meta.exitCode`)
    - Documents per-command payload keys (items for ghost/inventory/mcp, buckets for trend)
    - Documents `projectPaths: string[]` field added by Task 3 for mcp command
    - Contains jq examples for all 4 commands
    - `README.md` contains a "Machine-readable output" section linking to `docs/JSON-SCHEMA.md`
    - `docs/manual-test-results.md` has entries #2/#3/#4 reframed as spec clarifications referencing JSON-SCHEMA.md
    - The summary table row for section 5 (--json structured output) is updated from FAIL to PASS (spec clarified)
    - `apps/ccaudit/src/cli/_output-mode.ts` is unchanged (code not modified)
  </done>
  <acceptance_criteria>
    - `test -f docs/JSON-SCHEMA.md` exits 0
    - `grep -q "meta.exitCode" docs/JSON-SCHEMA.md` exits 0
    - `grep -q "meta.timestamp" docs/JSON-SCHEMA.md` exits 0
    - `grep -q "camelCase" docs/JSON-SCHEMA.md` exits 0
    - `grep -q "buckets" docs/JSON-SCHEMA.md` exits 0
    - `grep -q "projectPaths" docs/JSON-SCHEMA.md` exits 0
    - `grep -q "gh\` CLI" docs/JSON-SCHEMA.md` exits 0 OR `grep -q "gh CLI" docs/JSON-SCHEMA.md` exits 0
    - `grep -q "JSON-SCHEMA.md" README.md` exits 0
    - `grep -q "Machine-readable output" README.md` exits 0
    - `grep -q "JSON-SCHEMA" docs/manual-test-results.md` exits 0
    - `grep -q "spec clarification" docs/manual-test-results.md` exits 0
    - `test $(wc -l < docs/JSON-SCHEMA.md) -le 120` exits 0
    - `git diff apps/ccaudit/src/cli/_output-mode.ts | wc -l` returns 0 (code unchanged)
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 7: Extend --json arg description in all 4 command files to reference docs/JSON-SCHEMA.md (Gap #3 Category A4)</name>
  <files>
    apps/ccaudit/src/cli/commands/ghost.ts
    apps/ccaudit/src/cli/commands/inventory.ts
    apps/ccaudit/src/cli/commands/mcp.ts
    apps/ccaudit/src/cli/commands/trend.ts
  </files>
  <read_first>
    /Users/helldrik/.claude/plans/stateless-watching-koala.md
    apps/ccaudit/src/cli/commands/ghost.ts
    apps/ccaudit/src/cli/commands/inventory.ts
    apps/ccaudit/src/cli/commands/mcp.ts
    apps/ccaudit/src/cli/commands/trend.ts
  </read_first>
  <action>
    Gap #3 Category A4 fix: each command currently has a `json` arg block with `description: 'Output as JSON'`. Extend each to reference the schema doc so future testers/automation users can discover the contract from `--help`.

    **Edit 1: `apps/ccaudit/src/cli/commands/ghost.ts` (around lines 52-57)**

    Current:
    ```typescript
    json: {
      type: 'boolean',
      short: 'j',
      description: 'Output as JSON',
      default: false,
    },
    ```

    Change to:
    ```typescript
    json: {
      type: 'boolean',
      short: 'j',
      description: 'Output as JSON (see docs/JSON-SCHEMA.md for schema)',
      default: false,
    },
    ```

    **Edit 2: `apps/ccaudit/src/cli/commands/inventory.ts` (around lines 35-40)**

    Same change: replace `description: 'Output as JSON'` with `description: 'Output as JSON (see docs/JSON-SCHEMA.md for schema)'`.

    **Edit 3: `apps/ccaudit/src/cli/commands/mcp.ts` (around lines 51-56)**

    Same change.

    **Edit 4: `apps/ccaudit/src/cli/commands/trend.ts` (around lines 35-40)**

    Same change.

    After all 4 edits, validate:
    ```bash
    grep -c "docs/JSON-SCHEMA.md" apps/ccaudit/src/cli/commands/ghost.ts
    grep -c "docs/JSON-SCHEMA.md" apps/ccaudit/src/cli/commands/inventory.ts
    grep -c "docs/JSON-SCHEMA.md" apps/ccaudit/src/cli/commands/mcp.ts
    grep -c "docs/JSON-SCHEMA.md" apps/ccaudit/src/cli/commands/trend.ts
    ```
    Expected: each returns 1 (one match per file).

    The change is purely a string literal — no type or runtime behavior changes, no test changes required beyond the help-output test (Task 5) which only greps for `no-color`.

    Do NOT modify any other field in the json arg block (short alias `j`, type, default) — only the description.

    Do NOT touch any other command arg (since, verbose, quiet, csv, ci, live, timeout, dryRun) — only the `json` description.
  </action>
  <verify>
    <automated>grep -q "docs/JSON-SCHEMA.md" apps/ccaudit/src/cli/commands/ghost.ts && grep -q "docs/JSON-SCHEMA.md" apps/ccaudit/src/cli/commands/inventory.ts && grep -q "docs/JSON-SCHEMA.md" apps/ccaudit/src/cli/commands/mcp.ts && grep -q "docs/JSON-SCHEMA.md" apps/ccaudit/src/cli/commands/trend.ts</automated>
  </verify>
  <done>
    - Each of the 4 command files has `docs/JSON-SCHEMA.md` in the `--json` arg description
    - No other changes to any arg or runtime code
    - All files still parse as TypeScript
  </done>
  <acceptance_criteria>
    - `grep -q "description: 'Output as JSON (see docs/JSON-SCHEMA.md for schema)'" apps/ccaudit/src/cli/commands/ghost.ts` exits 0
    - `grep -q "description: 'Output as JSON (see docs/JSON-SCHEMA.md for schema)'" apps/ccaudit/src/cli/commands/inventory.ts` exits 0
    - `grep -q "description: 'Output as JSON (see docs/JSON-SCHEMA.md for schema)'" apps/ccaudit/src/cli/commands/mcp.ts` exits 0
    - `grep -q "description: 'Output as JSON (see docs/JSON-SCHEMA.md for schema)'" apps/ccaudit/src/cli/commands/trend.ts` exits 0
    - `pnpm -r typecheck 2>&1 | grep -c "error TS"` returns 0 (no type errors introduced)
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 8: Full verification gate — rebuild, run all tests, coverage, smoke-test, confirm all 4 gaps closed</name>
  <files>
  </files>
  <read_first>
    /Users/helldrik/.claude/plans/stateless-watching-koala.md
    .planning/phases/06-output-control-polish/06-VERIFICATION.md
  </read_first>
  <action>
    This task is pure verification — no file modifications. It proves all four escaped gaps from Phase 6 VERIFICATION.md are closed by running the canonical verification gate from the approved plan's "Verification plan" section.

    **Phase A — Rebuild and functional smoke tests**

    ```bash
    # Fresh build (critical: Task 5's help-output test also rebuilds; re-run here to catch
    # any regression from Tasks 6/7 that might affect the build)
    pnpm -F ccaudit build
    echo "BUILD_EXIT=$?"
    ```
    Expected: exit 0.

    **Category A — docs/JSON-SCHEMA discoverability (Gap #3)**
    ```bash
    # A1 - schema doc exists
    test -f docs/JSON-SCHEMA.md && wc -l docs/JSON-SCHEMA.md

    # A2 - README links to it
    grep -q 'JSON-SCHEMA.md' README.md && echo "README_OK"

    # A3 - per-command --json descriptions reference the schema
    for cmd in ghost inventory mcp trend; do
      node apps/ccaudit/dist/index.js $cmd --help 2>&1 | grep -q 'JSON-SCHEMA' || echo "FAIL: $cmd help missing JSON-SCHEMA"
    done
    ```
    Expected: schema doc exists, README_OK printed, no FAIL lines for any command.

    **Category B1 — --no-color visible in --help (Gap #4)**
    ```bash
    node apps/ccaudit/dist/index.js --help 2>&1 | grep -q 'no-color' && echo "ROOT_OK"
    for cmd in ghost inventory mcp trend; do
      node apps/ccaudit/dist/index.js $cmd --help 2>&1 | grep -q 'no-color' || echo "FAIL: $cmd help missing no-color"
    done

    # Regression guard: --no-color still works functionally at runtime
    node apps/ccaudit/dist/index.js ghost --no-color 2>&1 | cat | grep -c $'\e\[' || echo "0 ANSI sequences (expected)"
    NO_COLOR=1 node apps/ccaudit/dist/index.js ghost 2>&1 | cat | grep -c $'\e\[' || echo "0 ANSI sequences via env var (expected)"
    ```
    Expected: ROOT_OK printed, no FAIL lines, 0 ANSI sequences in both piped cases (the grep -c returns 0 when the pattern is not found, treated as a false match → the `|| echo` fallback fires).

    **Category B2 — mcp --csv no longer shows duplicate rows (Gap #5)**
    ```bash
    # Extract the name column (column 1) of the CSV body, sort, find duplicates.
    # On the tester's real inventory this previously showed duplicates for at least
    # one server. After the fix, the uniq -d output must be empty.
    node apps/ccaudit/dist/index.js mcp --csv 2>/dev/null | awk -F, 'NR>1 {print $1}' | sort | uniq -d
    # Expected: empty output (no duplicates).

    # JSON output preserves projectPaths array for traceability (new field from Task 3)
    node apps/ccaudit/dist/index.js mcp --json 2>/dev/null | jq '.items[0] | has("projectPaths")'
    # Expected: true (if at least one MCP server exists in the inventory).
    ```

    **Category B3 — pnpm -r build works from any directory (Gap #6)**
    ```bash
    pnpm -r build 2>&1 | tee /tmp/ccaudit-gap6-root.log
    echo "ROOT_BUILD_EXIT=$?"
    ! grep -q "ERR_PNPM_RECURSIVE_RUN_NO_SCRIPT" /tmp/ccaudit-gap6-root.log && echo "NO_SKIP_ERROR_ROOT"

    (cd packages/internal && pnpm -r build 2>&1 | tee /tmp/ccaudit-gap6-internal.log)
    echo "INTERNAL_BUILD_EXIT=$?"
    ! grep -q "ERR_PNPM_RECURSIVE_RUN_NO_SCRIPT" /tmp/ccaudit-gap6-internal.log && echo "NO_SKIP_ERROR_INTERNAL"

    (cd packages/terminal && pnpm -r build 2>&1 | tee /tmp/ccaudit-gap6-terminal.log)
    echo "TERMINAL_BUILD_EXIT=$?"
    ! grep -q "ERR_PNPM_RECURSIVE_RUN_NO_SCRIPT" /tmp/ccaudit-gap6-terminal.log && echo "NO_SKIP_ERROR_TERMINAL"
    ```
    Expected: three "BUILD_EXIT=0" lines and three "NO_SKIP_ERROR_*" lines printed.

    **Phase B — Workspace-wide regression gate**

    ```bash
    # Typecheck still passes across all packages
    pnpm -r typecheck
    echo "TYPECHECK_EXIT=$?"

    # Full test suite — must be >= 357 baseline + new tests from Tasks 2/3/4/5
    pnpm exec vitest --run
    echo "VITEST_EXIT=$?"
    ```
    Expected: both exit 0. Test count >= 357 (baseline was 357; Tasks 2/3/4/5 add tests, so final count should be approximately 357 - 1 (flipped test) + 1 (replacement 'has no-color') + 1 (new 'no-color description') + 2 (Task 3 aggregate) + 3 (Task 4 E2E) + 5 (Task 5 help) = ~368, but any count >= 357 is acceptable).

    ```bash
    # Coverage gate — Phase 6 thresholds (statements 80, branches 70, functions 80, lines 80)
    rm -rf coverage && pnpm exec vitest --run --coverage
    echo "COVERAGE_EXIT=$?"
    ```
    Expected: exit 0. The config thresholds in `vitest.config.ts` fail the run if any metric drops below.

    ```bash
    # Lint + format still clean
    pnpm lint && echo "LINT_OK"
    pnpm format:check && echo "FORMAT_OK"
    ```
    Expected: both print their OK marker.

    **Phase C — Real-world smoke test (the exact commands that surfaced the gaps)**

    ```bash
    # Rebuild once more to ensure dist/ reflects final state
    cd apps/ccaudit && pnpm build && cd -

    # --help now shows --no-color (Gap #4 closure)
    node apps/ccaudit/dist/index.js --help | grep -q 'no-color' && echo "GAP4_CLOSED"

    # mcp --csv no duplicates (Gap #5 closure)
    DUPS=$(node apps/ccaudit/dist/index.js mcp --csv 2>/dev/null | awk -F, 'NR>1 {print $1}' | sort | uniq -d | wc -l | tr -d ' ')
    test "$DUPS" = "0" && echo "GAP5_CLOSED (0 duplicate rows)"

    # JSON schema discoverable (Gap #3 closure)
    test -f docs/JSON-SCHEMA.md && grep -q 'JSON-SCHEMA.md' README.md && echo "GAP3_CLOSED"

    # jq with corrected camelCase paths works
    node apps/ccaudit/dist/index.js ghost --json 2>/dev/null | jq '.items | length' > /dev/null && echo "jq_items_OK"
    node apps/ccaudit/dist/index.js ghost --json 2>/dev/null | jq '.meta.timestamp' > /dev/null && echo "jq_meta_timestamp_OK"
    node apps/ccaudit/dist/index.js ghost --json 2>/dev/null | jq '.meta.exitCode' > /dev/null && echo "jq_meta_exitCode_OK"
    ```

    **Result aggregation:**

    Expected final output includes all of these markers:
    - BUILD_EXIT=0
    - ROOT_OK, NO FAIL lines for any subcommand help
    - 0 duplicate rows from mcp --csv
    - NO_SKIP_ERROR_{ROOT,INTERNAL,TERMINAL}
    - TYPECHECK_EXIT=0
    - VITEST_EXIT=0 with >= 357 tests passed
    - COVERAGE_EXIT=0 with all thresholds met
    - LINT_OK, FORMAT_OK
    - GAP3_CLOSED, GAP4_CLOSED, GAP5_CLOSED
    - jq_items_OK, jq_meta_timestamp_OK, jq_meta_exitCode_OK

    If ANY check fails, STOP and investigate. Do not mark this task done until all markers are printed.

    **No file modifications in this task** — it is the final verification gate.
  </action>
  <verify>
    <automated>pnpm -F ccaudit build && test -f docs/JSON-SCHEMA.md && grep -q 'JSON-SCHEMA.md' README.md && node apps/ccaudit/dist/index.js --help 2>&1 | grep -q 'no-color' && node apps/ccaudit/dist/index.js ghost --help 2>&1 | grep -q 'no-color' && node apps/ccaudit/dist/index.js inventory --help 2>&1 | grep -q 'no-color' && node apps/ccaudit/dist/index.js mcp --help 2>&1 | grep -q 'no-color' && node apps/ccaudit/dist/index.js trend --help 2>&1 | grep -q 'no-color' && node apps/ccaudit/dist/index.js ghost --help 2>&1 | grep -q 'JSON-SCHEMA' && pnpm -r build 2>&1 | tee /tmp/ccaudit-final-build.log && ! grep -q "ERR_PNPM_RECURSIVE_RUN_NO_SCRIPT" /tmp/ccaudit-final-build.log && pnpm -r typecheck && pnpm exec vitest --run 2>&1 | tee /tmp/ccaudit-final-vitest.log && grep -q "passed" /tmp/ccaudit-final-vitest.log && rm -rf coverage && pnpm exec vitest --run --coverage 2>&1 | tee /tmp/ccaudit-final-cov.log && test -f coverage/coverage-summary.json && pnpm lint && pnpm format:check</automated>
  </verify>
  <done>
    - `pnpm -F ccaudit build` exits 0 (fresh dist reflects all prior task changes)
    - `docs/JSON-SCHEMA.md` exists; `README.md` links to it (Gap #3 closed)
    - `--no-color` visible in `--help` for root + all 4 subcommands (Gap #4 closed)
    - `JSON-SCHEMA.md` mentioned in each command's `--json` help description (Gap #3 Category A4 closed)
    - `mcp --csv` shows 0 duplicate rows; `mcp --json` items have `projectPaths` field (Gap #5 closed)
    - `pnpm -r build` exits 0 from workspace root AND from both subpackage directories with no ERR_PNPM_RECURSIVE_RUN_NO_SCRIPT (Gap #6 closed)
    - `pnpm -r typecheck` exits 0
    - `pnpm exec vitest --run` exits 0 with >= 357 tests passing (baseline + new tests)
    - `pnpm exec vitest --run --coverage` exits 0 with all thresholds met (80/70/80/80)
    - `pnpm lint` and `pnpm format:check` exit 0
    - `_output-mode.ts` buildJsonEnvelope code unchanged (D-16 preserved)
    - `scan-mcp.ts` per-project dedup unchanged (Phase 8 RMED-06 preserved)
    - `color.ts` initColor runtime contract unchanged (D-07 preserved)
    - No new issues introduced in adjacent code
  </done>
  <acceptance_criteria>
    - `pnpm -F ccaudit build` exits 0
    - `test -f docs/JSON-SCHEMA.md` exits 0
    - `grep -q 'JSON-SCHEMA.md' README.md` exits 0
    - `node apps/ccaudit/dist/index.js --help 2>&1 | grep -q 'no-color'` exits 0
    - `node apps/ccaudit/dist/index.js ghost --help 2>&1 | grep -q 'no-color'` exits 0
    - `node apps/ccaudit/dist/index.js inventory --help 2>&1 | grep -q 'no-color'` exits 0
    - `node apps/ccaudit/dist/index.js mcp --help 2>&1 | grep -q 'no-color'` exits 0
    - `node apps/ccaudit/dist/index.js trend --help 2>&1 | grep -q 'no-color'` exits 0
    - `node apps/ccaudit/dist/index.js ghost --help 2>&1 | grep -q 'JSON-SCHEMA'` exits 0
    - `node apps/ccaudit/dist/index.js mcp --csv 2>/dev/null | awk -F, 'NR>1 {print $1}' | sort | uniq -d | wc -l | tr -d ' '` returns 0 (no duplicate server names)
    - `pnpm -r build 2>&1 | grep -c ERR_PNPM_RECURSIVE_RUN_NO_SCRIPT` returns 0
    - `(cd packages/internal && pnpm -r build)` exits 0
    - `(cd packages/terminal && pnpm -r build)` exits 0
    - `pnpm -r typecheck` exits 0
    - `pnpm exec vitest --run 2>&1 | grep -Eq "([0-9]+) passed"` and the number is >= 357
    - `rm -rf coverage && pnpm exec vitest --run --coverage` exits 0 and creates `coverage/coverage-summary.json`
    - `pnpm lint` exits 0
    - `pnpm format:check` exits 0
    - `git diff apps/ccaudit/src/cli/_output-mode.ts | wc -l` returns 0
    - `git diff packages/internal/src/scanner/scan-mcp.ts | wc -l` returns 0
    - `git diff packages/terminal/src/color.ts | wc -l` returns 0
  </acceptance_criteria>
</task>

</tasks>

<verification>
- `test -f docs/JSON-SCHEMA.md` exits 0 (schema doc created)
- `wc -l docs/JSON-SCHEMA.md` returns <= 120 (Category A2 size constraint)
- `grep -q 'JSON-SCHEMA.md' README.md` exits 0 (Category A3 link)
- `grep -q 'JSON-SCHEMA' docs/manual-test-results.md` exits 0 (Category A1 correction)
- `grep -q "'no-color':" apps/ccaudit/src/cli/_shared-args.ts` exits 0 (Gap #4 declaration)
- `grep -q "not.toHaveProperty('no-color')" apps/ccaudit/src/cli/_shared-args.ts` exits 1 (old test flipped)
- `grep -q "aggregateMcpByName" apps/ccaudit/src/cli/commands/mcp.ts` exits 0 (Gap #5 helper)
- `grep -q "projectPaths" apps/ccaudit/src/cli/commands/mcp.ts` exits 0 (JSON traceability field)
- `grep -q '"build": "tsc"' packages/internal/package.json` exits 0 (Gap #6 internal stub)
- `grep -q '"build": "tsc"' packages/terminal/package.json` exits 0 (Gap #6 terminal stub)
- `grep -q "docs/JSON-SCHEMA.md" apps/ccaudit/src/cli/commands/ghost.ts` exits 0 (Category A4 help text, ghost)
- `grep -q "docs/JSON-SCHEMA.md" apps/ccaudit/src/cli/commands/inventory.ts` exits 0 (Category A4, inventory)
- `grep -q "docs/JSON-SCHEMA.md" apps/ccaudit/src/cli/commands/mcp.ts` exits 0 (Category A4, mcp)
- `grep -q "docs/JSON-SCHEMA.md" apps/ccaudit/src/cli/commands/trend.ts` exits 0 (Category A4, trend)
- `test -f apps/ccaudit/src/__tests__/help-output.test.ts` exits 0 (Task 5 regression guard)
- `test -f apps/ccaudit/src/__tests__/mcp-aggregation.test.ts` exits 0 (Task 4 regression guard)
- `pnpm -r build` exits 0 from workspace root (Gap #6 verified)
- `(cd packages/internal && pnpm -r build)` exits 0 (Gap #6 subpackage verified)
- `(cd packages/terminal && pnpm -r build)` exits 0 (Gap #6 subpackage verified)
- `pnpm -r typecheck` exits 0 (no regressions)
- `pnpm exec vitest --run 2>&1 | grep -Eq "([0-9]+) passed"` AND the number is >= 357 (baseline preserved + new tests)
- `pnpm exec vitest --run --coverage` exits 0 with `coverage/coverage-summary.json` present (thresholds met)
- `pnpm lint` exits 0
- `pnpm format:check` exits 0
- `node apps/ccaudit/dist/index.js --help | grep -q 'no-color'` exits 0 (Gap #4 E2E verified)
- `node apps/ccaudit/dist/index.js mcp --csv 2>/dev/null | awk -F, 'NR>1 {print $1}' | sort | uniq -d | wc -l | tr -d ' '` returns 0 (Gap #5 E2E verified)
- `git diff apps/ccaudit/src/cli/_output-mode.ts | wc -l` returns 0 (D-16 preserved)
- `git diff packages/internal/src/scanner/scan-mcp.ts | wc -l` returns 0 (Phase 8 RMED-06 preserved)
- `git diff packages/terminal/src/color.ts | wc -l` returns 0 (D-07 runtime contract preserved)
</verification>

<success_criteria>
- All four escaped gaps from Phase 6 VERIFICATION.md are closed: Gap #3 (JSON schema discoverability), Gap #4 (--no-color help visibility), Gap #5 (mcp CSV cross-project duplicates), Gap #6 (pnpm -r build from subpackages)
- D-16 JSON envelope is preserved: zero code changes to `_output-mode.ts` buildJsonEnvelope; the locked test at lines 120-145 continues passing unchanged
- D-07 color runtime contract is preserved: `initColor()` still reads `process.argv` directly as the authoritative runtime source; gunshi declaration is additive for help metadata only
- Phase 8 RMED-06 traceability is preserved: `scan-mcp.ts` per-project dedup Set is unchanged; scanner still emits one InventoryItem per (projectPath, serverName) pair so remediation can rewrite the correct config keys
- Test baseline preserved: 357 tests → >= 357 after flipped test + new tests from Tasks 2/3/4/5; no regressions in any existing test
- Coverage gate preserved: Phase 6 thresholds 80/70/80/80 (statements/branches/functions/lines) still met per `vitest.config.ts`
- Every one of the 12 files in `files_modified` exists and is touched exactly as specified; no files outside this list are modified
- Tester workflow restored: a fresh tester running the same 15-section manual test from `docs/manual-test-results.md` now sees consistent behavior matching the documentation, with `docs/JSON-SCHEMA.md` available as the canonical reference for JSON consumers
- OUTP-02 (NO_COLOR env + --no-color flag) is now functionally AND discoverably satisfied
- OUTP-06 (--json export on all read commands) is now functionally AND schema-documented
- OUTP-07 (--csv export on all read commands) is now functionally correct (no duplicate rows) for the mcp subcommand
</success_criteria>

<output>
After completion, create `.planning/phases/06-output-control-polish/06-07-SUMMARY.md` following the standard GSD summary template. Record:
- Which gaps were closed and how
- Files modified (all 12)
- Test count before (357) and after
- Coverage numbers after
- Any unexpected issues encountered during execution
- Confirmation that D-16, D-07, and Phase 8 RMED-06 invariants were preserved
</output>
